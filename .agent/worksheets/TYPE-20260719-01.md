# Type the NVIDIA Cosmos request contract

<!-- agent-summary: This worksheet records a focused TypeScript contract improvement. -->
<!-- agent-summary: The target is the NVIDIA Cosmos provider request body. -->
<!-- agent-summary: The test previously captured the request body as `any`. -->
<!-- agent-summary: The provider now exports its request-body type for reuse. -->
<!-- agent-summary: The provider test captures and parses the body with that type. -->
<!-- agent-summary: Full workspace typecheck and focused provider tests are required. -->
<!-- agent-summary: Repository lint and validation results are recorded before handoff. -->

## Goal

Replace the `any` request-body capture in the NVIDIA Cosmos provider test with
the provider's typed request contract.

## Research

- The repository is a strict TypeScript monorepo with `pnpm typecheck` as the
  aggregate typecheck command.
- `apps/api/src/lib/generative/providers/nvidia-cosmos.ts` already defines the
  request payload shape locally, but the test used `body: any` when capturing
  the outgoing request.
- No independent reviewer agent/tool is configured in this session; the
  repository validation and targeted tests are the available review gates.

## Plan

1. Export the existing NVIDIA Cosmos request-body type.
2. Use it for the captured body in the provider test.
3. Run the targeted test, package/aggregate typechecks, lint fix, and agent
   validation.

## Changes

- Exported `NvidiaCosmosRequestBody` from the provider module.
- Replaced the provider test's `body: any` capture with the exported type.

## Validation

- `pnpm exec tsx --test src/lib/generative/__tests__/nvidia-cosmos.test.ts` from
  `apps/api`: passed (2 tests).
- `pnpm typecheck`: passed (8 packages).
- `pnpm --filter @popcorn/api test -- src/lib/generative/__tests__/nvidia-cosmos.test.ts`:
  the focused test passed, but the package test glob also ran the full API suite
  and exposed two unrelated pre-existing guest-retention failures.
- `pnpm agent:lint:fix`: passed.
- `pnpm agent:validate -- --scope api`: passed.
- `git diff --check`: passed.

## Feedback

- No independent reviewer agent/tool was available in this session.
- The repository checks found no issue with the focused change.

## Handoff

Ready for an open PR. The change is intentionally limited to one external API
request contract and its test capture.
