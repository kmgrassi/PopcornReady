# Worksheet: WEB-20260716-02

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/<WORKSHEET_ID> as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

- Keep the project storyboard board visually focused: each ready beat shows its image and beat title, without the full generation prompt in the card.
- Let a creator click a storyboard asset to inspect its saved generation prompt in the asset dialog, while preserving the existing Request Changes flow.
- Add behavior-focused browser coverage for the compact board and asset prompt disclosure.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, `docs/repository-structure.md`
- `apps/web/PRODUCT.md`, `apps/web/DESIGN.md`, `docs/ui-interaction-model.md`
- `docs/testing/e2e-test-inventory-and-gaps.md`, `apps/web/e2e/README.md`

## Decisions

- The prompt belongs to the asset-detail dialog as read-only provenance, not in a beat card or prefilled into the change-request input.
- Scope the change to the dedicated project storyboard route; the project overview already uses image-and-scene preview cards without prompt copy.

## Changes

- Removed the inline prompt drawer from the ready storyboard beat card.
- Added a read-only saved-generation-prompt section to the asset Request Changes dialog.
- Kept the change-request textarea empty when a ready asset is opened; missing-asset generation continues to use its existing prompt seed.
- Added desktop and mobile browser coverage and recorded it in the E2E inventory.

## Validation evidence

- `pnpm install --frozen-lockfile` completed because this worktree initially had no dependencies.
- `pnpm --filter @popcorn/web typecheck` passed.
- `pnpm agent:lint:fix` passed.
- `pnpm --filter @popcorn/web test:e2e e2e/storyboard-editor.spec.ts --project=chromium` passed (2 tests).
- `pnpm --filter @popcorn/web test:e2e e2e/storyboard-editor.spec.ts --project=mobile-safari --project=mobile-chrome` passed (2 tests).
- `pnpm agent:validate -- --scope web` passed.
- Playwright's local API process logged unrelated missing-Supabase worker configuration warnings; the mocked storyboard browser tests passed.

## Independent reviews

- Research/plan: `/root/research_review` confirmed the prompt leak and recommended read-only modal provenance.
- Implementation: `/root/research_review` approved with no blocking findings.
- Wrap-up: `/root/research_review` approved the final implementation, worksheet, feedback record, and validation evidence.

## Blockers and risks

- None identified.

## Next action / handoff

- Commit the implementation, test, worksheet, feedback record, and inventory update together.
