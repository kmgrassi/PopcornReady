# API-20260728-PR12 — creator-direct domain API

<!-- agent-summary: Implements the protected creator-direct proposal and run-status API. -->
<!-- agent-summary: Requests are discriminated by product outcome and are mapped server-side to a domain task. -->
<!-- agent-summary: Video edits require an authorized project source asset before a proposal can be created. -->
<!-- agent-summary: Confirmation consumes the existing one-use proposal gate and returns the durable session and run identities. -->
<!-- agent-summary: Creator-direct reads and cancellation are actor and project scoped. -->
<!-- agent-summary: The route reuses the PR 6 domain-run service and PR 9 proposal-gate database RPCs. -->
<!-- agent-summary: API type checking and repository validation pass after installing the locked workspace dependencies. -->

## Goal

Implement PR 12's protected API boundary in a dedicated route group without a
new persistence stack.

## Decisions

- The route accepts product outcome kinds, never a client-supplied domain or raw `DomainTask.v1`.
- Project ownership is checked before source/reference assets are used.
- The existing creator-direct gate RPC owns one-use confirmation and enqueueing.

## Changed files

- `apps/api/src/routes/v1/agent-creations.ts`
- `apps/api/src/routes/v1/protected-routes.ts`

## Validation

- `pnpm install --offline --frozen-lockfile` — passed.
- `pnpm --filter @popcorn/api typecheck` — passed.
- `pnpm agent:lint:fix` — passed.
- `pnpm agent:validate -- --scope api` — passed.
- `pnpm --filter @popcorn/api test` — repository suite ran 1,051 tests; 3 pre-existing failures from missing historical guest-retention migrations and an existing public-project UUID expectation.

## Review

Independent reviewers were not available: this task's execution policy does not permit spawning subagents unless requested.
