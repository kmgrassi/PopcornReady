# Worksheet: QA-20260730-01

<!-- agent-summary: Scopes production browser testing that agents can run safely and repeatably. -->
<!-- agent-summary: The scope separates deployed read smoke, sandboxed mutations, and provider canaries. -->
<!-- agent-summary: Release identity, control-plane security, cleanup, and cost isolation precede mutations. -->
<!-- agent-summary: User-visible browser operations continue through authenticated APIs so RLS is exercised. -->
<!-- agent-summary: Existing production-test sandbox primitives are reused without enabling the dev router. -->
<!-- agent-summary: Manual-test drift is repaired where it conflicts with the active Vite route table. -->
<!-- agent-summary: Documentation validation and four independent review checkpoints are recorded here. -->

## Goal and acceptance criteria

Create an implementation-ready scope for agent-operated browser testing against
the deployed Netlify, Railway, Supabase, S3/CloudFront, and provider paths.

Acceptance requires:

- an explicit definition of safe production coverage and excluded irreversible
  behavior;
- a phased PR plan with dependencies, tests, rollback boundaries, and done
  criteria;
- release-coherence, authentication, tenancy, cleanup, cost, concurrency, and
  evidence requirements;
- an executable-agent flow contract and production coverage matrix;
- repair of active manual-testing documentation that directly conflicts with
  the current route table or tested behavior;
- stabilization of the review-gate approval/rejection browser test discovered
  by the PR's Web E2E check;
- independent research, plan, implementation, and wrap-up review;
- `pnpm agent:lint:fix` and
  `pnpm agent:validate -- --scope web`.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/agent-system/README.md`
- `docs/agent-system/documentation-contract.md`
- `docs/agent-system/reviews.md`
- `docs/agent-system/testing-policy.md`
- `docs/agent-system/performance-and-visual-regression.md`
- `docs/testing/e2e-test-inventory-and-gaps.md`
- `docs/manual-tests/full-app-inventory.md`
- `docs/manual-tests/browser-flow-backlog.md`
- `apps/web/e2e/README.md`
- `apps/web/playwright.config.ts`
- `.github/workflows/web-e2e.yml`
- `.github/workflows/deploy-api.yml`
- `apps/api/src/lib/test-sandboxes/README.md`
- `apps/api/src/routes/v1/dev-web-e2e.ts`
- `supabase/migrations/20260615130000_production_test_sandboxes.sql`
- `docs/scopes/database-access-boundary.md`
- `docs/supabase-identity-and-rls.md`
- `docs/railway-deployment.md`
- `seed/production-fixtures/README.md`

## Decisions

- Keep fast deterministic local Playwright and local-Supabase integration
  coverage; production coverage adds a distinct tier rather than replacing it.
- Define "test everything in production" as exercising deployed production
  binaries and services against isolated test identities and workspaces, never
  customer data.
- Require one release identity across the deployed web, API, and database
  readiness contract before a production browser run starts.
- Add a separate remote Playwright configuration that starts no local servers
  and fails closed on an unexpected host or release.
- Do not enable or repurpose the existing dev-only sandbox router in
  production. Use a separately threat-modeled, service-authenticated control
  plane.
- Provision one ephemeral auth/domain identity and owned workspace per mutating
  sandbox; browser operations use the normal request-scoped Supabase path so RLS
  remains under test.
- Use typed direct-Postgres transaction modules for new trusted multi-table
  control-plane workflows. Do not add new application workflow RPC targets.
- Treat database deletion as only one part of cleanup. Jobs, storage, provider
  artifacts, notifications, analytics, webhooks, credits, and late workers
  require explicit handling.
- Keep terminal production-browser run evidence in a durable record that
  survives the cascade deletion of the ephemeral `test_sandboxes` row.
- Public-sharing automation requires an unlisted-canary contract inside an
  `internal_test` workspace; it never broadens deletion to normal user
  workspaces.
- Keep real provider work opt-in and budgeted. Keep irreversible charges,
  destructive account/admin behavior, and fault injection out of production.
- Store living coverage separately from immutable per-release evidence.

## Changes

- Added `docs/scopes/production-browser-agent-testing.md` with goals, safety
  invariants, target architecture, release and control-plane contracts,
  sandbox/cleanup lifecycle, executable flow charters, coverage matrix, evidence
  schema, cadence, operations, and eight ordered PRs.
- Linked the proposal from the agent-system README and testing policy while
  labeling it as proposed rather than active authority.
- Reconciled the E2E route inventory and manual route map with active auth
  callback, Activity, storyboard redirect, project media, project-detail section
  fallback, and dev harness routes.
- Removed the contradictory instruction that a no-output Watch route both stays
  and redirects.
- Added the required seven-line summary headers to the materially updated manual
  guide and browser-flow backlog.
- Removed obsolete review-gate modal compatibility branches from
  `run-progress.spec.ts`, synchronized approval and rejection with their exact
  successful POST responses, and asserted the resulting visible gate state.
- Updated the sandbox lifecycle so terminal status and evidence survive
  cascading workspace deletion, and made unlisted canaries the only permitted
  automated production-sharing design.
- Labeled the dated specialist production verification as historical
  point-in-time evidence.
- Added the task feedback record.

## Validation evidence

- Live production health on 2026-07-30 returned deployed commit
  `d291350f6a0cc7e70a9c0c84f1b5b3051d114200`, matching the checkout at research
  time.
- `git diff --check` — passed.
- Active route reconciliation against `apps/web/src/App.tsx` confirmed the
  added auth callback, Activity, project media, and five dev harness routes.
- `apps/web/e2e/project-watch-empty.spec.ts` and the E2E inventory confirm the
  direct no-output Watch route remains in place with an explanation.
- `pnpm agent:lint:fix` — passed.
- `pnpm agent:validate -- --scope docs` — passed, including agent lint, two
  migration-validator tests, and validation of all 87 migration filenames.
- After the PR review fixes, `pnpm agent:lint:fix` and
  `pnpm agent:validate -- --scope docs` passed again with the same two migration
  tests and 87-file migration validation.
- The original Web E2E run `30562343388` failed the review-gate action test on
  mobile Chrome and marked Chromium/mobile Safari flaky because the helper
  selected a nonexistent `What should change?` modal path or observed feedback
  before the response-driven transition completed.
- `CI=1 pnpm --filter @popcorn/web exec playwright test
  e2e/run-progress.spec.ts --grep "submits review-gate approve and reject
  actions with notes" --project chromium --project mobile-safari --project
  mobile-chrome --repeat-each=3` — 9 passed in 22.6 seconds.
- `pnpm agent:lint:fix` — passed for the four-file E2E follow-up.
- `pnpm agent:validate -- --scope web` — passed, including agent lint, both
  migration-validator tests, validation of 87 migration filenames, and the web
  TypeScript check.
- The unpatched replacement Web E2E run `30566335435` passed on commit
  `60a3011f`, confirming the original check was intermittent rather than caused
  by this PR's documentation diff. The deterministic helper correction still
  removes the diagnosed race.

## Independent reviews

- Research review `/root/research_review` confirmed that hosted Playwright still
  starts local web/API servers, CI has no deployed browser pass, production
  sandbox primitives are disconnected from browser testing, and the manual
  route/behavior inventory has drift.
- Plan review `/root/research_review` required release coherence before remote
  smoke; a control-plane threat model before mutations; full side-effect
  cleanup; cost isolation; sensitive evidence redaction; explicit read-only
  side effects; unique run-scoped concurrency; and an explicit route/flow
  registry rather than brittle JSX scraping.
- Implementation review `/root/research_review` initially found that fixed QA
  membership could not select a sandbox under the current one-workspace auth
  model; evidence security landed too late; zero-provider flows included
  dispatching actions; release/schema identity was ambiguous; global settings
  could leak across identities; external provider retention was overstated; and
  final acceptance was not finite. The scope now uses one ephemeral identity per
  mutating sandbox, moves minimum evidence security into PR 2, classifies
  mutations, uses a shared orchestration SHA plus distinct artifact hashes and
  an ordered migration-prefix digest, separates user-global state, records
  external retention explicitly, and requires a checked coverage inventory.
- Implementation rereview then found that ephemeral identity creation would
  trigger real promotional credits before workspace classification, while the
  example charter and visibility flow still contradicted the revised safety
  model. PR 3 now classifies the identity through trusted Admin app metadata
  before user-creation triggers, suppresses internal-test grants/accounting,
  compensates partial Auth/database provisioning, constrains audit output, keeps
  the zero-budget charter project/draft-only, and proves internal fixtures remain
  unauthenticated-denied. Final rereview returned READY with no remaining
  security, tenancy, database-boundary, sequencing, or testability blocker.
- Wrap-up review `/root/research_review` returned READY. It confirmed the diff is
  limited to the proposed scope, its process records, agent-system discovery,
  and targeted manual/E2E inventory corrections; all prior findings remain
  resolved; the proposal grants no live authority; validation claims match the
  worktree; and no PR-readiness blocker remains.
- PR #843 review added four actionable findings: cascading sandbox deletion
  would erase terminal evidence, a normal-purpose sharing workspace lacked safe
  cleanup authority, two mounted routes were still absent from the reconciled
  inventory, and the materially updated manual guide lacked summary lines. All
  four are addressed in the follow-up diff; independent rereview is pending.
- Independent follow-up review required the durable record to forbid
  cascade/restrict relationships through every ephemeral identifier, including
  `sandbox_id`, and required the unlisted canary to use a server-validated,
  resource-scoped, expiring, revocable capability rather than obscurity. Both
  clarifications and their required negative/migration tests are now in scope;
  final rereview returned READY and confirmed no remaining blocker.
- CI-failure research/plan review confirmed the test's immediate visibility
  probe could select an obsolete modal branch before the current review gate
  rendered. It required exact successful POST synchronization and observable
  approve/reject state assertions.
- CI-failure implementation review returned READY after confirming that the
  obsolete branches were removed, response waits are registered before clicks,
  request bodies remain asserted, and the repeated three-browser run is
  proportionate evidence for the flake.
- CI-failure wrap-up review returned READY. It confirmed the four-file diff is
  limited to the flaky test, living E2E inventory, and task records; the failed
  and passing GitHub runs are characterized truthfully; and the targeted
  browser and repository validation evidence supports handoff.

## Blockers and risks

- Production mutation work must not begin until the release identity and
  control-plane threat model are implemented and reviewed.
- Public-sharing behavior is intentionally unavailable to `internal_test`
  workspaces under current visibility policies; PR 6 must implement the scoped
  unlisted-canary contract before automating that flow.
- Current authentication resolves one owned workspace per domain user.
  Mutating production tests therefore require one ephemeral auth/domain identity
  per sandbox unless a separate product-level workspace selector is implemented.

## Next action / handoff

Commit and push the E2E stabilization to PR #843, then verify the replacement
Web E2E run.
