# Type boundary hardening — TYPE-20260720-01

<!-- agent-summary: Hardens ambiguous JSON parsing boundaries in the API OpenAI adapter. -->
<!-- agent-summary: Keeps parsed provider payloads as unknown until runtime validation. -->
<!-- agent-summary: Scope is limited to apps/api and its workflow records. -->
<!-- agent-summary: Existing object guards remain the source of truth for provider payloads. -->
<!-- agent-summary: Targeted API typecheck and adapter tests are required before handoff. -->
<!-- agent-summary: Repository lint and validation are required before commit. -->
<!-- agent-summary: Review feedback and final commands are recorded below. -->

## Goal and acceptance criteria

Improve one ambiguous TypeScript boundary in active code, preserve behavior, and open an open PR.

## Context and source-of-truth documents

- Read `AGENT_WORKFLOW.md`, `CLAUDE.md`, `docs/repository-structure.md`, and `docs/agent-system/README.md`.
- The repository uses strict TypeScript. `apps/api/src/lib/llm/openai.ts` had unannotated `JSON.parse` results at two provider-response boundaries.

## Decisions

- Annotate both parsed values as `unknown`; rely on the existing `isJsonObject` guard before assigning or returning them.
- Keep the change bounded to the adapter because existing tests already cover valid, malformed, and non-object tool arguments.

## Changes

- [x] Annotate structured tool arguments parse result as `unknown`.
- [x] Annotate tool-choice arguments parse result as `unknown`.
- [x] Add feedback record.

## Validation evidence

- `pnpm install --frozen-lockfile` passed; dependencies were absent initially.
- `pnpm --filter @popcorn/api typecheck` passed.
- `pnpm exec tsx --test apps/api/src/lib/llm/__tests__/openai-adapter.test.ts` passed: 14 tests passed.
- `pnpm agent:lint:fix` passed; no product changes were made by the fixer.
- `pnpm agent:validate -- --scope api` passed.
- `git diff --check` passed.
- `pnpm --filter @popcorn/api test -- src/lib/llm/__tests__/openai-adapter.test.ts` was also attempted, but the package script ignores the extra path and ran the full API suite; two unrelated pre-existing guest-retention migration tests failed.

## Independent reviews

- Independent review was unavailable at research, plan, implementation, and wrap-up: `AGENT_REVIEW_COMMAND` is not configured in this environment. The repository review adapter was invoked for all four checkpoints and returned its documented configuration error.

## Blockers and risks

None known. The change is compile-time only and does not alter runtime parsing.

## Next action / handoff

Committed implementation, worksheet, and feedback; next action is to push the branch and open the PR.
