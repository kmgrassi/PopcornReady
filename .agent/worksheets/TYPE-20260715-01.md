# Type boundary hardening — TYPE-20260715-01

<!-- agent-summary: Hardens an ambiguous JSON parsing boundary in the OpenAI LLM adapter. -->
<!-- agent-summary: Keeps parsed provider payloads as unknown until validated. -->
<!-- agent-summary: Scope is limited to packages/llm and its workflow worksheet. -->
<!-- agent-summary: Existing runtime object guards remain the source of truth. -->
<!-- agent-summary: Targeted package typecheck and tests are required before handoff. -->
<!-- agent-summary: Repository validation and lint are required before commit. -->
<!-- agent-summary: Review feedback and final commands are recorded below. -->

## Goal

Improve one ambiguous TypeScript boundary in the active monorepo and prevent the ambiguity from returning.

## Research

- Read `AGENT_WORKFLOW.md`, `CLAUDE.md`, and `docs/repository-structure.md`.
- Baseline `pnpm typecheck` was unavailable because dependencies are not installed (`turbo: command not found`).
- Candidate: `packages/llm/src/openai.ts` had two unannotated `JSON.parse` results, which are typed as `any` by the standard library.

## Plan

- Identify a focused `any`/ambiguous type in active code. (Done: OpenAI tool-argument parsing.)
- Replace it with an explicit domain type and targeted test.
- Run the affected package path, lint/validation, and independent review checkpoints.

## Changes

- Annotated both parsed JSON values as `unknown` so they must pass the existing `isJsonObject` guard before use.
- Existing OpenAI parser tests cover valid object payloads and rejection of non-object payloads.

## Validation

- `pnpm install --frozen-lockfile` passed; dependencies were absent initially.
- `pnpm --filter @popcorn/llm typecheck` passed.
- `pnpm --filter @popcorn/llm test` passed: 30 tests passed.
- `pnpm agent:lint:fix` passed.
- `pnpm agent:validate -- --scope all` passed, including web and API typechecks.
- `git diff --check` passed.
- `pnpm typecheck` was attempted before dependency installation and failed because `turbo` was unavailable; the full scoped validation passed after installation.

## Review / feedback

- Independent review was unavailable at research, plan, implementation, and wrap-up: `AGENT_REVIEW_COMMAND` is not configured in this environment. The repository review adapter was invoked once and returned its documented configuration error.

## Handoff

Commit the implementation, worksheet, and feedback record, tag `worksheet/TYPE-20260715-01`, and open a non-draft PR.
