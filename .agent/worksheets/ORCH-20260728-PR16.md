# Worksheet: ORCH-20260728-PR16

<!-- agent-summary: Durable record for specialist-agent orchestration PR 16. -->
<!-- agent-summary: PR 16 atomically fans root work out to Visuals and Audio and joins durable reports. -->
<!-- agent-summary: The branch is based on the completed PR 15 head from GitHub PR 822. -->
<!-- agent-summary: One batch action owns child causation, budget admission, and the root join state. -->
<!-- agent-summary: Session serialization remains per domain while different sessions may run concurrently. -->
<!-- agent-summary: Late reports must remain fenced by immutable pins and durable claim generations. -->
<!-- agent-summary: Validation covers concurrency, restarts, join idempotency, budgets, and session isolation. -->

## Goal and acceptance criteria

Implement PR 16 from `docs/scopes/specialist-agent-orchestration-prs.md`: a root-only `delegate_domains` tool atomically creates independent Visuals and Audio runs, parks the root on a durable join, and resumes exactly once after required terminal reports.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, and `docs/repository-structure.md`.
- `docs/NORTH_STAR.md` and PR 16 in `docs/scopes/specialist-agent-orchestration-prs.md`.
- Completed PR 15, GitHub PR #822 (`6f999039`), and its root/profile prerequisites.
- `apps/api/src/lib/orchestrator/engine.ts`, `domain-run-service.ts`, `delegate-domain.ts`, and turn-boundary migrations.

## Decisions

- The initial worktree was rebased onto PR #822 after the user confirmed it was complete.
- Keep serial `delegate_visuals` and `delegate_audio` semantics intact; batched dispatch is an additional root-only capability.

## Changes

- Added root-only `delegate_domains` alongside the serial dispatch tools.
- Added one batch RPC that creates exactly one Visuals and one Audio child inside
  one transaction, using the existing finite-run/session/idempotency machinery.
- Added a join guard on the existing root action: early reports leave it
  running; the last terminal report applies it with all reported output assets.
- Extended engine reconciliation to load every child for an action and remain
  parked until the durable join resolves.
- Added focused parsing/catalog/migration tests.

## Validation evidence

- `git fetch origin pull/822/head:codex/pr15-request-changes-822` and rebase onto `6f999039` succeeded.
- `pnpm install --frozen-lockfile` passed.
- API typecheck and focused catalog, delegation, and migration tests passed.
- `pnpm agent:lint:fix` and `pnpm agent:validate -- --scope api` passed.

## Independent reviews

- Research: unavailable in this session because no independent reviewer is configured and delegation is disallowed by the active collaboration policy.

## Blockers and risks

- The existing terminal-report RPC marks its parent delegation action terminal immediately. A batch join must defer that transition until all required child reports arrive.
- No local Supabase instance is available in this task environment, so the new
  transaction/trigger is covered structurally rather than by a live DB smoke.

## Next action / handoff

- PR #823 review follow-up: added proposal idempotency, empty-project routing,
  strict rerun coverage/UUID validation, and serialized failure-aware fan-in.
- Focused API/web tests and `pnpm agent:validate -- --scope all` passed.
