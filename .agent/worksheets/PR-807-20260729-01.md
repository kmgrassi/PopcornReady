# Worksheet: PR-807-20260729-01

<!-- agent-summary: Durable record for resolving the merge conflict on pull request 807. -->
<!-- agent-summary: Reconcile the route-helper refactor with the current main branch. -->
<!-- agent-summary: Preserve the PR's public helper exports and existing route behavior. -->
<!-- agent-summary: Validate the focused API route suite and repository API checks. -->
<!-- agent-summary: Record the merge commit, resulting checks, and any baseline limitations. -->
<!-- agent-summary: Keep the corresponding feedback entry and this worksheet in the resolution commit. -->
<!-- agent-summary: Do not include secrets, customer data, or unverified test claims. -->

## Goal and acceptance criteria

Resolve the GitHub merge conflicts for PR #807 (`Split orchestrator run route helpers`) against current `main`, preserving both the extracted helper boundary and newer orchestrator-run behavior.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/agent-system/reviews.md`
- PR #807

## Decisions

- Merged `origin/main` into the PR branch rather than rebasing so the existing PR history remains intact.
- Chose the current-main `orchestrator-run-board-revisions.ts` and `orchestrator-runs.ts` implementations because main already contains the equivalent helper refactor plus later finite-run and run-detail behavior.
- Removed the PR-only `orchestrator-run-control.ts`, whose helpers now live in the main-owned route, board-revision, and restart modules.

## Changes

- Resolved add/add conflict for `orchestrator-run-board-revisions.ts` in favor of the current-main boundary, retaining operator diagnostics and insufficient-credit helpers.
- Resolved `orchestrator-runs.ts` in favor of the current-main boundary, retaining newer hierarchical detail, runtime controls, and restart extraction.
- Removed obsolete duplicate `orchestrator-run-control.ts`.
- Added this worksheet and `PR-807-20260729-01` feedback record.

## Validation evidence

- `pnpm install --frozen-lockfile` passed.
- `pnpm --filter @popcorn/api typecheck` passed after dependencies were installed.
- Focused route tests passed: `orchestrator-runs.test.ts` (30), `orchestrator-run-observability.test.ts` (8), `restart-from-stage.test.ts` (7), and `finite-run-cancel-route.test.ts` (1).
- `NODE_ENV=development pnpm --filter @popcorn/api dev` started the API; `GET /api/v1/health` returned HTTP 200 with `status: "ok"`.
- `pnpm agent:lint:fix` passed.
- `pnpm agent:validate -- --scope api` passed.

## Independent reviews

- `AGENT_REVIEW_COMMAND` is not configured, so the required independent-review adapter was unavailable for research, plan, implementation, and wrap-up checkpoints.

## Blockers and risks

- No unresolved merge conflicts remain.
- The PR's original refactor is now largely subsumed by current main; the merge commit primarily advances the branch and records its conflict resolution.

## Next action / handoff

- Commit the merge, tag the worksheet, push the PR branch, and verify GitHub mergeability.
