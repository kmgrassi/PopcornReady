# Worksheet: WEB-20260714-01

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/WEB-20260714-01 as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

Refactor one large active-code file into smaller route-owned components. Acceptance:
`HomePage.tsx` and `HomePage.module.css` are below the 1,000-line threshold,
behavior is preserved, the affected web path is validated, and an open PR is
created.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `apps/web/PRODUCT.md`
- `apps/web/DESIGN.md`
- `docs/ui-interaction-model.md`
- Impeccable skill context and product-register guidance

## Decisions

- Chose `apps/web/src/routes/HomePage.tsx` and `HomePage.module.css` because they
  were at or above the large-file threshold and live in the active web app.
- Split render-heavy landing sections into route-owned files under
  `apps/web/src/routes/home/`.
- Kept upload/auth/navigation state in `HomePage.tsx` and passed callbacks into
  `LandingPromptComposer` to avoid splitting the state machine across files.
- Kept landing global CSS overrides and keyframes in one module to avoid breaking
  CSS-module keyframe rewriting.

## Changes

- Added `homeContent.ts` for static landing/pricing constants.
- Added `LandingPromptComposer.tsx` for the hero prompt/upload form.
- Added `LandingComparisonHeatmap.tsx` for the competitive heatmap.
- Added `LandingPricing.tsx` for pricing cards and note.
- Added `AccountChoiceModal.tsx` for the account/guest modal.
- Reduced `HomePage.tsx` from 998 lines to 430 lines.
- Reduced `HomePage.module.css` from 1,011 lines to 985 lines by trimming
  oversized comments while preserving CSS behavior.

## Validation evidence

- `pnpm install` to restore missing workspace dependencies in this worktree.
- `pnpm --filter @popcorn/web typecheck` passed.
- `pnpm --filter @popcorn/web test` passed: 31 tests passed.
- `pnpm agent:lint:fix` passed and formatted 7 changed files.
- `pnpm agent:validate -- --scope web` passed.
- Ran Vite on `http://localhost:3001/` because port 3000 was occupied.
- Browser smoke with system Chrome checked `/` at desktop and mobile:
  prompt fill, upload/record toggle, pricing heading, heatmap cell visibility,
  submit enabled state, and console/page errors. No errors reported.
- Captured top-of-page render and verified hero DOM styles programmatically.

## Independent reviews

- Research/plan review: flagged CSS-module keyframe split risk and prompt/upload
  state-machine coupling. Plan adjusted to keep keyframes together and extract
  composer as one callback-driven component.
- Implementation review: found new split files were untracked. Resolved by
  including the new route-owned files in the commit. No other extraction
  regressions reported.

## Blockers and risks

- Playwright-managed Chromium revision expected by the installed package was not
  present in the local cache. Used installed system Chrome for browser smoke.
- Modal backdrop behavior was not fully smoke-tested because local auth and
  guest-run state made the scripted modal path unreliable; Escape behavior is
  preserved in the extracted component by moving the existing code unchanged.

## Next action / handoff

Commit the implementation, worksheet, and feedback log, tag
`worksheet/WEB-20260714-01`, then open an open PR for review.
