# Worksheet: API-20260719-01

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/API-20260719-01 as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

Refactor one active code file over 1000 lines into smaller files, keep behavior unchanged, validate the affected API path, and open a ready-for-review PR.

Acceptance criteria:

- `apps/api/src/routes/v1/orchestrator-runs.ts` is under 1000 lines.
- Extracted code has cohesive route-helper ownership.
- Existing public helper exports used by tests continue to work.
- Targeted route tests and API validation pass or blockers are documented.
- PR is open, not draft.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/supabase-identity-and-rls.md`
- `docs/scopes/orchestrator-cutover-prs.md`

## Decisions

- Chose `apps/api/src/routes/v1/orchestrator-runs.ts` because it was active API code at 1117 lines.
- Avoided legacy `src/` and legacy global CSS targets.
- Kept route registration in `orchestrator-runs.ts` to preserve route order and mounted paths.
- Extracted pure/helper policy into sibling route files and re-exported tested helpers from the original route module.

## Changes

- Added `apps/api/src/routes/v1/orchestrator-run-board-revisions.ts` for board feedback parsing, payload/proposal shaping, and resume policy.
- Added `apps/api/src/routes/v1/orchestrator-run-control.ts` for stop-after gates, storyboard continuation, credit failure checks, and restart selection helpers.
- Reduced `apps/api/src/routes/v1/orchestrator-runs.ts` from 1117 to 852 lines.

## Validation evidence

- `pnpm install --frozen-lockfile` passed after the worktree was found to have no `node_modules`.
- Initial `pnpm --filter @popcorn/api typecheck` failed before install because `tsc` was not available.
- Initial package-script test invocation accidentally ran the whole API test suite and failed on known unrelated migration/discover tests plus a missing re-export. The re-export was fixed and focused tests were rerun with `pnpm --filter @popcorn/api exec tsx --test ...`.
- `pnpm --filter @popcorn/api typecheck` passed.
- `pnpm --filter @popcorn/api exec tsx --test src/routes/v1/__tests__/orchestrator-runs.test.ts src/routes/v1/__tests__/orchestrator-run-observability.test.ts src/routes/v1/__tests__/restart-from-stage.test.ts` passed: 45 tests.
- `pnpm agent:lint:fix` passed after adding required summary lines to the feedback record.
- `pnpm dev:api` started the Express server and logged `[api] Express listening on :4000 (test)`. Background worker ticks logged missing Supabase env (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`), expected for this checkout; the HTTP server drained cleanly on SIGINT.
- Re-ran `pnpm --filter @popcorn/api typecheck` after lint; passed.
- Re-ran focused route tests after lint; passed: 45 tests.
- `pnpm agent:validate -- --scope api` passed.

## Independent reviews

- Research review: independent reviewer agreed the target was appropriate and recommended extracting board revision helpers, restart helpers, and preserving route registration/export behavior.
- Implementation review: independent reviewer found no behavior, import/export, route-order, or focused coverage issues.

## Blockers and risks

- None currently known in the implementation.
- Whole API package test script currently runs unrelated failing tests in this checkout when extra paths are appended; use direct `tsx --test` for focused route coverage.

## Next action / handoff

Commit, tag, push, and open the PR.
