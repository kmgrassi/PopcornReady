# Worksheet: API-20260723-01

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/API-20260723-01 as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

Tighten the provider API-key request-body type boundary with one focused production change and regression coverage. Acceptance: no unchecked body cast remains in that boundary, focused tests pass, repository validation is run, and an open PR is created.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/agent-system/README.md`, `docs/agent-system/reviews.md`, `docs/agent-system/worksheets-and-feedback.md`

## Decisions

- Scope is limited to `apps/api/src/routes/v1/provider-api-keys.ts` and its focused tests.
- Preserve the route's existing runtime validation and API error behavior while replacing the broad `Record<string, unknown>` assertion with a named request shape and type guard.
- Independent reviewer was not available in the configured environment; record the attempted review command and compensate with targeted tests plus `pnpm agent:validate`.

## Changes

- Added `ProviderApiKeyRequestBody` and returned it from `readBodyObject` instead of asserting `Record<string, unknown>`.
- Added focused tests for valid bodies, rejected primitive/array bodies, and invalid API-key values.

## Validation evidence

- `pnpm --filter @popcorn/api exec tsc --noEmit` — passed.
- `pnpm --filter @popcorn/api exec tsx --test src/routes/v1/__tests__/provider-api-keys.test.ts` — 3 passed.
- `pnpm typecheck` — all 8 workspace packages passed.
- `pnpm agent:lint:fix` and `pnpm agent:validate -- --scope api` — passed.
- API affected path: started the API on port 4417 and `curl --fail http://127.0.0.1:4417/api/v1/health` returned `{"status":"ok","authMode":"local"...}`.
- `pnpm --filter @popcorn/api test` — new tests passed, with two unrelated existing guest-retention migration tests failing: `guest retention migration only purges expired anonymous-owned projects` and `anonymous-user purge migration never deletes claimed users or surviving projects' owners`.
- `git diff --check` — passed.

## Independent reviews

- Research and implementation review commands were attempted, but `AGENT_REVIEW_COMMAND` is not configured (exit 3). No independent reviewer was available in this environment.

## Blockers and risks

The API dev server logs expected Supabase configuration warnings in local mode; its health endpoint still served successfully. Full API suite retains the two unrelated migration failures listed above.

## Next action / handoff

Commit the implementation and records, push, and open the non-draft PR.
