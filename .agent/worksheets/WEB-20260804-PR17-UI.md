# Worksheet: WEB-20260804-PR17-UI

<!-- agent-summary: Complete the PR 17 creator UI rollout for the hierarchy run projection. -->
<!-- agent-summary: Show one Creative Director with compact Visuals and Audio work lanes. -->
<!-- agent-summary: Consume hierarchy responses in both run detail and restored Studio generation. -->
<!-- agent-summary: Keep completed work collapsed and primitive activity behind disclosure. -->
<!-- agent-summary: Preserve the existing root approval loop and Asset Studio boundary. -->
<!-- agent-summary: Validate polling, desktop/mobile behavior, runtime guards, and legacy fallback. -->
<!-- agent-summary: Commit implementation, tests, documentation, worksheet, and feedback together. -->

## Goal and acceptance criteria

Finish the PR 17 web rollout so hierarchy-backed full-video runs no longer look
like a flat tool pipeline. Creators can identify the Creative Director, see the
state and outputs of Visuals and Audio work, understand root-owned questions,
and optionally inspect sanitized production activity on desktop and mobile.
Older responses without `hierarchy` keep the existing progress UI, and
creator-direct Asset Studio behavior remains separate.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `apps/web/PRODUCT.md`
- `apps/web/DESIGN.md`
- `docs/ui-interaction-model.md`
- `docs/NORTH_STAR.md`
- `docs/scopes/specialist-agent-orchestration-prs.md`
- `docs/testing/e2e-test-inventory-and-gaps.md`
- `apps/web/e2e/README.md`
- Impeccable craft, responsive, and audit guidance

## Decisions

- Use the user-approved quiet Variant B: a divider-based director summary with
  Visuals/Audio lanes and the existing semantic palette.
- Lead with live hierarchy state before the approved-plan recap on mobile.
- Collapse complete/canceled lanes to checked summaries; expand current or
  attention-needed work; keep finite actions/jobs in a closed disclosure.
- Do not expose raw hierarchy identifiers, chain-of-thought, or a specialist
  answer field. Root approval and cancellation keep their existing actions.
- Runtime-validate hierarchy-bearing responses at the shared API boundary and
  fall back to the current projection when hierarchy is absent.

## Changes

- Added the typed hierarchy response contract, runtime guards, creator-facing
  summarizers, and unit coverage.
- Added a responsive CSS Module hierarchy panel and integrated it into run
  detail and restored Studio generation.
- Replaced hierarchy-backed primitive stage/tool summaries while preserving the
  legacy response path.
- Added browser fixtures for states, outputs, collapse/disclosure, root-owned
  questions, polling updates, Studio restoration, and mobile containment.
- Updated the interaction, design, roadmap, and E2E source-of-truth documents.

## Validation evidence

- `pnpm --filter @popcorn/web typecheck` — passed.
- `pnpm --filter @popcorn/web test` — passed, 74 tests.
- Focused Chromium hierarchy/Studio Playwright run — passed, 4 tests.
- Full affected Chromium Playwright files — passed, 18 tests.
- Focused mobile Chrome + mobile Safari hierarchy and restored-Studio run —
  passed, 6 tests.
- Impeccable detector — passed with no findings after removing a width
  transition from the progress fill.
- `pnpm agent:lint:fix` — passed.
- `pnpm agent:validate -- --scope web` — passed, including agent lint, workflow
  policy tests, migration tests/validation, and web typecheck.
- PR-review validation: web unit suite passed 76 tests; full run-progress
  Chromium suite passed 14 tests; the hierarchy diagnostics case passed in
  mobile Chrome and mobile Safari; `pnpm agent:lint:fix` and
  `pnpm agent:validate -- --scope web` passed again.
- Impeccable detection found only three pre-existing warnings in the shared
  `ProgressView.module.css` (two legacy width transitions and its established
  monospace declaration); the comment-fix markup/styles added no new finding.
- Desktop 1280px and mobile 412px screenshots inspected from the real run-detail
  route with deterministic browser API fixtures; the hierarchy had no document
  overflow. Inspection moved the live director summary before the plan recap so
  mobile creators encounter current work first.

## Independent reviews

- Research checkpoint: confirmed the API hierarchy was shipped but neither run
  detail nor restored Studio generation consumed it; Asset Studio is a separate
  creator-direct boundary.
- Plan checkpoint: approved the compact director/lane model, meaningful output
  counts and links, root approval preservation, sanitized detail, responsive
  coverage, and both web entry paths.
- Implementation checkpoint: found that state-priority selection could show a
  recovered run's blocked predecessor as current. The fix selects the latest run
  matching the API-projected session state, with a latest-run fallback; a
  blocked-predecessor/active-successor test proves stale progress is excluded.
  Re-review passed hierarchy/status unit tests and typecheck with no remaining
  blocker. Nonblocking findings also tightened job-progress validation, added
  lane live announcements and exact single-output navigation, removed a
  potentially conflicting hierarchy header meter, and added mobile Studio
  coverage.
- Wrap-up checkpoint: approved the full run-detail and restored-Studio rollout,
  hierarchy-absent and standalone fallbacks, root gate preservation, runtime
  validation, CSS Module/TanStack boundaries, browser coverage, and synchronized
  documentation with no blocker. Asset Studio follow-up controls remain a
  separate PR 13 scope, not a PR 17 regression.
- PR-review research/plan checkpoint: confirmed all three comments were valid
  and non-conflicting. The plan kept authorized operator detail outside the
  creator-safe panel, selected current work by explicit state priority, and
  aligned empty-hierarchy header copy with the existing planning state.
- PR-review implementation checkpoint: approved the extracted legacy/hierarchy
  diagnostics behavior, creator/operator separation, state priority, planning
  copy, scoped CSS, and focused tests with no blocker.
- PR-review wrap-up checkpoint: confirmed all three threads are fully addressed,
  the reused diagnostics stay authorized/collapsed/separate, focused unit and
  desktop/mobile browser coverage is coherent, and final validation is recorded
  with no blocker.

## Blockers and risks

- The existing mobile app-shell breadcrumb is sticky and can appear across a
  stitched full-page screenshot; viewport checks and overflow assertions remain
  authoritative for this scoped component.
- Live provider work was not started; deterministic route fixtures exercise the
  actual browser entry points without spend.
- PR-review visual inspection exercised the hierarchy run-detail entry point
  with expanded authorized diagnostics at 1280px desktop and 412px mobile.
  The disclosure remained visually subordinate, technical data stayed
  contained, and the page had no horizontal overflow. The stitched mobile
  screenshot retained the known sticky-breadcrumb artifact described above.

## Next action / handoff

Commit the review fixes, move the worksheet tag, and push the branch update to
ready PR 884. Leave review replies/resolution to the user unless explicitly
requested.
