# Worksheet: GEN-20260715-02

<!-- agent-summary: PR 789 follow-up fixes recovery timestamp precision and Netlify secret scanning. -->
<!-- agent-summary: Recovery claims must re-check staleness atomically inside PostgreSQL. -->
<!-- agent-summary: JavaScript-normalized timestamps are not safe equality tokens for timestamptz rows. -->
<!-- agent-summary: The claim RPC uses the current database updated_at value and a stale-before boundary. -->
<!-- agent-summary: Redaction fixtures must exercise behavior without resembling deployable credentials. -->
<!-- agent-summary: Validate migration SQL transactionally against local Supabase without resetting data. -->
<!-- agent-summary: Use worksheet/GEN-20260715-02 as the git tag after completion. -->

## Goal and acceptance criteria

Address the unresolved P1 review thread and failing Netlify deploy on PR 789.

- A stale queued or running job can be claimed even when PostgreSQL stored
  `updated_at` with microsecond precision.
- The claim remains tenant/project scoped, atomic, status guarded, and
  service-role only.
- The operator-redaction test remains behaviorally meaningful without a
  scanner-visible provider-key literal.
- Targeted tests, the production web build, local migration compilation, and
  repository validation pass before pushing the existing PR branch.

## Evidence and decisions

- GitHub review thread `PRRT_kwDOSqQ6Xc6RLn59` identified the lossy equality
  between PostgreSQL `timestamptz` and `Job.updatedAt` normalized by JavaScript.
- Netlify deploy `6a57bcb5f8f98c0009524f3a` reported a single enhanced
  secret-scan match in `orchestrator-run-observability.test.ts` line 182.
- GitHub Actions `smoke` passed; the four failed PR statuses all pointed to the
  same external Netlify deploy.
- The claim now uses a service-only `claim_job_recovery` SQL function that
  atomically checks `updated_at <= p_stale_before` in the database. It no longer
  sends normalized `updatedAt` back as an equality predicate.
- The fake diagnostic uses an `api_key` value that exercises redaction without
  resembling a live provider credential.

## Validation evidence

- API typecheck passed.
- Focused recovery, migration, and projection suite passed: 16 tests.
- The forward migration compiled successfully against local Supabase inside a
  transaction that was rolled back.
- `pnpm --filter @popcorn/web... build` passed.
- Local API started and `/api/v1/health` returned HTTP 200.
- `pnpm agent:lint:fix` passed.
- `pnpm agent:validate -- --scope all` passed.
- `git diff --check` passed.

## Reviews

- Research/plan review: the independent GitHub Codex review supplied the P1
  precision finding and the implementation follows its required invariant.
- Additional sub-agent review is unavailable for this follow-up because the
  active session does not authorize delegation; targeted regression and full
  repository validation are required before handoff.
- Implementation/wrap self-review verified that the SQL function is
  service-role only, tenant/project/status scoped, and atomically re-checks the
  current database timestamp. A competing claim refreshes `updated_at`, so a
  second caller cannot claim the same stale row.

## Next action

Commit, tag, and push the validated fixes to PR 789, then recheck its statuses.
