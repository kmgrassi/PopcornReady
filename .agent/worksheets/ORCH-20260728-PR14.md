# Worksheet: ORCH-20260728-PR14

<!-- agent-summary: Implements roadmap PR 14's creative-director root profile behind a temporary flag. -->
<!-- agent-summary: The flagged root receives only root ownership and durable Visuals/Audio dispatch tools. -->
<!-- agent-summary: Fresh graph projections and run cost context preserve graph IDs as the creative-state channel. -->
<!-- agent-summary: The flat all-tools root remains the exact default behavior while the hierarchy flag is off. -->
<!-- agent-summary: Recovery enables both domain roles only with the same root hierarchy rollout fence. -->
<!-- agent-summary: North Star principles and the accepted domain contract now record the approved two-level hierarchy. -->
<!-- agent-summary: Focused API tests, validation, smoke, review, and feedback are recorded before handoff. -->

## Goal and acceptance criteria

Implement PR 14 from `docs/scopes/specialist-agent-orchestration-prs.md` on the combined PR 10/11 prerequisite branch. The root must become the flag-gated creative director: exact root ownership plus domain dispatch, fresh graph context, and no leaf Visuals/Audio provider tools. The legacy flat root remains the default.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, `docs/repository-structure.md`
- `docs/NORTH_STAR.md`, `docs/domain-agent-orchestration-contract.md`
- `docs/scopes/specialist-agent-orchestration-prs.md`
- `docs/agent-system/README.md`

## Decisions

- Base the isolated branch on the combined PR 10/11 prerequisite merge commit.
- Use `POPCORN_CREATIVE_DIRECTOR_HIERARCHY` as an opt-in temporary root flag; no flag preserves the flat production registry, prompt, and empty context.
- Root gets fresh graph projection plus durable run budget/spend context; prior root actions retain origin-filtered delegated reports at their turn boundary.
- The recovery worker enables both specialist domains only when the same flag is on, preventing Audio dispatches from being stranded.

## Changes

- Added the flag-gated creative-director prompt/profile, exact root registry,
  fresh root graph projection, and current root budget/spend context.
- Kept the flat root registry/prompt/no-context behavior when the flag is off.
- Wired the same flag through recovery so hierarchy work enables both Visuals
  and Audio; documented that the deployment flag remains stable while durable
  hierarchy work drains.
- Promoted the hierarchy record in the North Star, domain contract, and roadmap.
- Addressed PR #819 review feedback: the root context now carries compact,
  root-origin terminal domain reports, and Visuals delegation derives required
  terminal outputs from its explicitly bounded output kinds.

## Validation evidence

- Focused API tests: 29 passed (agent definitions, feature flags, recovery
  roles, and capability partition).
- `pnpm --filter @popcorn/api typecheck` — passed.
- `pnpm agent:lint:fix` and `pnpm agent:validate -- --scope api` — passed.
- API smoke: started on port 4401 and `GET /api/v1/health` returned status ok.
- Rebased onto PR 13's merged PR 12 integration commit; 28 focused API
  orchestration tests and 34 web tests (including Asset Studio coverage) pass,
  followed by `pnpm agent:validate -- --scope all`.
- PR review follow-up: focused API suite (including root-report isolation and
  bounded Visuals output requirements) and API type-check pass.

## Independent reviews

- Research: completed. The independent reviewer identified the dormant root registry/context seam, flag propagation requirement, Audio worker trap, and documentation/test requirements. Its findings are incorporated.
- Plan: completed as part of the independent research review; no unresolved plan findings.
- Implementation: completed. The independent reviewer confirmed the root
  boundary and identified rollout-drain and root-projection follow-ups; the
  bounded deployment rule is documented and the PR 17 projection follow-up is
  recorded in feedback.
- Wrap-up: completed after final targeted checks.

## Blockers and risks

- This is a stacked PR: it requires PR 10 and PR 11 to merge first.

## Next action / handoff

- Force-push the rebased branch and verify GitHub mergeability.
