# Worksheet: ORCH-20260728-PR18

<!-- agent-summary: Durable record for the PR 18 creative-director rollout gate. -->
<!-- agent-summary: New root runs pin creative_director while legacy rows remain flat. -->
<!-- agent-summary: Emergency fallback requires an explicit flag and a future UTC expiry. -->
<!-- agent-summary: Asset Studio stays independently gated by VITE_STANDALONE_CREATION_ENABLED. -->
<!-- agent-summary: Worker, health, migration, and unit tests make the rollout observable and reversible. -->
<!-- agent-summary: Production parity, smoke, and soak evidence are still release operations, not local claims. -->
<!-- agent-summary: PR 19 may remove the fallback only after the documented seven-day soak. -->

## Goal and acceptance criteria

Implement PR 18's safe hierarchy rollout gate without changing the standalone
Asset Studio gate or allowing an emergency rollback to rewrite an active root.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, and `docs/repository-structure.md`.
- `docs/scopes/specialist-agent-orchestration-prs.md` (PR 18).
- `docs/scopes/gate-0-decision-record.md` and `docs/agent-system/creative-director-rollout.md`.

## Decisions

- Treat pre-PR-18 root rows with no profile as legacy flat runs.
- Pin every newly created root run to `creative_director` or `flat` before it can enqueue.
- Require both an emergency flag and a future UTC expiry for the flat fallback.
- Leave `VITE_STANDALONE_CREATION_ENABLED` untouched.

## Changes

- Kept the creative-director hierarchy opt-in until Gate-0 evidence clears and
  added an expiring fallback resolver for the activated profile.
- Added immutable `root_execution_profile` persistence and mapper support.
- Logged rollout selection at the worker boundary and exposed it via health.
- Added a production soak runbook with thresholds, owner, rollback, queries, and evidence requirements.

## Validation evidence

- Focused API suite passed: feature fallback, profile selection, worker attribution,
  and migration contract tests (24/24).
- `pnpm --filter @popcorn/api typecheck` passed.
- `pnpm agent:lint:fix` and `pnpm agent:validate -- --scope all` passed.
- `NODE_ENV=development PORT=4318 pnpm --filter @popcorn/api start` served
  `/api/v1/health` with `creativeDirectorHierarchy.enabled: true`.
- `pnpm db:local:status` completed, but no local database reset or billable
  provider run was authorized for this task.
- Follow-up review fixes passed the same focused suite (24/24), typecheck,
  lint, and scoped validation after rebasing onto merged PR 17. The local health
  smoke reports `creativeDirectorHierarchy.enabled: false` until Gate 0 clears.

## Independent reviews

- Research/plan review: Franklin reviewed the merged prerequisite surface and identified durable profile pinning as required for rollback safety.
- Implementation/wrap-up review: Franklin reviewed atomic anonymous admission,
  persisted-profile logging, UTC expiry validation, and the focused suite; the
  explicit-null RPC guard was corrected before handoff.
- Review-follow-up: three GitHub threads on PR #825 were addressed. No separate
  reviewer was available in this continuation after the thread review.

## Blockers and risks

- Gate-0 paired live decision evidence and the production seven-day soak require release-owner access and must not be represented as locally complete.

## Next action / handoff

- Run the listed checks, complete independent review, commit/tag/push, then record production evidence during the release soak.
