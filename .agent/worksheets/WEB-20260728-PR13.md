# Worksheet: WEB-20260728-PR13

<!-- agent-summary: Durable evidence for the PR 13 standalone Asset Studio web implementation. -->
<!-- agent-summary: The UI calls only PR 12's typed proposal, confirmation, and status endpoints. -->
<!-- agent-summary: Standalone Asset Studio remains hidden unless VITE_STANDALONE_CREATION_ENABLED is true. -->
<!-- agent-summary: The route shows Image, Video, and Soundtrack in creator language. -->
<!-- agent-summary: A server-issued proposal must be explicitly confirmed before dispatch. -->
<!-- agent-summary: Session status polling uses TanStack Query and terminal reports remain typed. -->
<!-- agent-summary: Production enablement still requires a matching API-side flag and fuller PR 12 operations. -->

## Goal and acceptance criteria

Implement the feature-gated Asset Studio entry route for PR 13 using PR 12's creator-direct proposal API.

## Context and source-of-truth documents

`AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, `apps/web/PRODUCT.md`, `apps/web/DESIGN.md`, `docs/ui-interaction-model.md`, and `docs/scopes/specialist-agent-orchestration-prs.md`.

## Decisions

Use `VITE_STANDALONE_CREATION_ENABLED` (default false); map creator language Image, Video, and Soundtrack to the server-owned task kinds. The UI only submits an intent and maximum cost, then requires the server-issued proposal token before dispatch.

## Changes

Added `/create`, a typed TanStack Query client for proposals/confirmation/status, and a responsive CSS Module surface with loading, error, blocked, question, and completed-output states.

## Validation evidence

`pnpm --filter @popcorn/web typecheck` and `pnpm --filter @popcorn/web test` passed. Dependency install was required for the new worktree.

## Independent reviews

Research and contract review identified the missing PR 12 prerequisite and then verified the actual PR 820 endpoint paths. Corrected terminal output/report typing to match the shared discriminated report contract.

## Blockers and risks

PR 12 currently provides no API-side standalone feature flag and does not yet expose optional references, question answers, blocked dependency actions, or Use-in-project operations; those must be added before enabling the web flag in production.

## Next action / handoff

Run repository lint/validation, browser QA, commit, push, and open a ready PR.
