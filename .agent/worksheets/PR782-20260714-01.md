# Worksheet: PR782-20260714-01

<!-- agent-summary: Durable record for the PR #782 review-fix task. -->
<!-- agent-summary: The task narrows creator-direct Visuals output capabilities by task kind. -->
<!-- agent-summary: The executable contract and compile-time fixtures are the primary changed files. -->
<!-- agent-summary: The architecture document records the user-visible capability mapping. -->
<!-- agent-summary: Validation uses shared typecheck, type tests, lint-fix, and agent validation. -->
<!-- agent-summary: The independent reviewer checkpoint is recorded before push. -->
<!-- agent-summary: Follow-up is to reply to and resolve the addressed GitHub review thread. -->

## Goal and acceptance criteria

Address PR #782's unresolved P2 review comment by making creator-direct
Visuals output kinds specific to `image_create`, `video_create`, and
`video_edit`, with compile-time coverage for each mapping.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/domain-agent-orchestration-contract.md`
- PR #782 review thread `PRRT_kwDOSqQ6Xc6Q05Qo`

## Decisions

- `image_create` authorizes only `image` outputs.
- `video_create` authorizes `clip`, `composite`, or `render` outputs.
- `video_edit` authorizes only `clip` outputs.
- Production Visuals tasks retain their broader output capability because they
  are outcome-level tasks rather than creator-direct primitive requests.

## Changes

- Split the creator-direct Visuals branch of `DomainTaskV1` into one branch per
  task kind and added the conditional output-kind mapping.
- Added compile-time assertions proving the three direct mappings.
- Updated the authoritative contract documentation with the same mapping.
- Added this worksheet and a feedback-log entry for the review fix.

## Validation evidence

- `pnpm install --frozen-lockfile` — passed in the isolated worktree.
- `pnpm --filter @popcorn/shared typecheck` — passed.
- `pnpm --filter @popcorn/shared test:types` — passed.
- `pnpm agent:lint:fix` — passed; only the two intended code files were changed.
- `pnpm agent:validate -- --scope all` — passed.
- `git diff --check` — passed.

## Independent reviews

- Independent reviewer found no blocking issue and confirmed the mappings match
  the repository docs. They recommended future negative `@ts-expect-error`
  fixtures when server-side derivation is implemented.

## Blockers and risks

- No known blockers. The exact `video_create` capability includes the existing
  video-producing output kinds, while `video_edit` remains clip-only.

## Next action / handoff

Commit and push the fix to PR #782, reply with `@Codex`, and resolve the review
thread only after the pushed commit is visible.
