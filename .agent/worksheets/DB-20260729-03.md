# Worksheet: DB-20260729-03

<!-- agent-summary: Repair the creator-direct pgcrypto resolution incident. -->
<!-- agent-summary: Repair creator-direct confirmation without rewriting applied migrations. -->
<!-- agent-summary: Qualify every affected pgcrypto digest call through the extensions schema. -->
<!-- agent-summary: Repair both confirmation and rejection even though rejection is currently latent. -->
<!-- agent-summary: Preserve the latest cancellation guard, grants, and one-use proposal semantics. -->
<!-- agent-summary: Validate migrations and execute both repaired RPC paths against local Postgres. -->
<!-- agent-summary: Link independent reviews, feedback, the final commit, tag, and ready PR here. -->

## Goal and acceptance criteria

Repair the production creator-direct proposal failure as an independent,
deployable incident PR:

- repair the production creator-direct confirmation failure with a forward
  migration that resolves pgcrypto functions explicitly;
- repair the same latent failure in creator-direct rejection;
- prove both repaired functions resolve pgcrypto correctly against a real local
  Supabase/Postgres database;
- publish the work as a ready-for-review pull request.

## Context and source-of-truth documents

`AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`,
`docs/repository-structure.md`, `docs/supabase-identity-and-rls.md`,
`docs/scopes/specialist-agent-orchestration-prs.md`,
`docs/agent-system/testing-policy.md`, and `docs/agent-system/reviews.md`.

## Decisions

- Use a new forward migration; never edit applied migrations.
- Recreate both affected functions with `extensions.digest(...)`.
- Preserve the latest queued-run/cancellation guard and existing privileges.
- Defer the database-access boundary and direct Postgres foundation to
  `DB-20260729-04` so `/create` recovery is not delayed.

## Changes

- Added a forward migration that recreates creator-direct confirmation and
  rejection with four schema-qualified `extensions.digest(...)` calls.
- Preserved the latest queued-run cancellation guard, transaction order,
  function signatures, SECURITY DEFINER search path, and service-only grants.
- Added structural coverage for both repaired functions, all four qualified
  pgcrypto calls, the queued-run guard, and privileges.
- Added an opt-in local Supabase integration regression that executes both
  service-role RPCs through PostgREST, asserts their intended domain errors,
  excludes the former digest-resolution error, and proves anon denial.

## Validation evidence

- `pnpm db:migrations:validate` — passed for 86 migrations.
- Targeted migration structure test — 3/3 passed.
- `pnpm --filter @popcorn/api typecheck` — passed.
- `pnpm db:migrations:test` — 2/2 passed.
- Clean local Supabase start applied the full migration chain through
  `20260729170000_creator_direct_pgcrypto_schema.sql`.
- Opt-in local integration test — 1/1 passed with
  `RUN_LOCAL_DB_INTEGRATION=1`; both service RPCs returned SQLSTATE `23514`
  domain errors and anon calls returned `42501`.
- Real API process against the rebuilt local Supabase stack returned HTTP 200
  from `/api/v1/health` and drained cleanly on SIGINT.
- `pnpm agent:lint:fix` — passed for 5 changed files.
- `pnpm agent:validate -- --scope api` — passed agent lint, migration tests,
  migration validation, and API typecheck.
- Full `pnpm --filter @popcorn/api test` — 950 passed, 119 skipped, 3 TODO, and
  4 unrelated baseline failures: two stale guest-retention migration filename
  reads, one stale public-project UUID fixture assertion, and one stale
  orchestrator catalog expectation missing `delegate_domains`.

## Independent reviews

- Research: recommended splitting the urgent incident repair from the direct
  Postgres foundation so `/create` recovery is not delayed; accepted.
- Plan: approved the repair-only PR with requirements to preserve the latest
  queued-run guard, qualify exactly four calls, assert real SQLSTATE/domain
  errors, and run the opt-in local test explicitly; all applied.
- Implementation: approved with no blocking findings. The reviewer verified
  semantic parity with both latest deployed function bodies, all four qualified
  calls, exact ACL/search-path preservation, local catalog state, and that the
  four full-suite failures are unrelated.
- Wrap-up: approved with no blocking findings. The reviewer confirmed the
  incident-only scope, semantic parity, validation evidence, task-scoped
  documentation, and the unrelated nature of all four full-suite failures.

## Blockers and risks

- Proposal confirmation remains blocked until this migration reaches
  production.
- The regression must execute the functions against Postgres; a regex-only
  migration test would not prove extension resolution.

## Next action / handoff

Commit, tag, push, and open the ready PR. Production verification remains
pending until the Supabase migration workflow completes after merge.
