# Worksheet: DB-20260730-02

<!-- agent-summary: Add database contract tests that catch runtime schema and authorization drift before production. -->
<!-- agent-summary: Cover retired relation references in the API without restoring legacy schema. -->
<!-- agent-summary: Exercise orchestrator context against the current local asset-graph and story-spine schema. -->
<!-- agent-summary: Separate schema-existence failures from request-RLS and trusted-role permission failures. -->
<!-- agent-summary: Keep provider-backed generation out of the required test path and assert zero paid tool work. -->
<!-- agent-summary: Make failures name the runtime operation, relation, role, and expected access boundary. -->
<!-- agent-summary: Run targeted API tests, a local database smoke, lint, and API agent validation. -->

## Goal and acceptance criteria

Create a focused series of automated database contract tests that would have
caught the production `orchestratorContext.listPanels` read of retired
`public.storyboard_panels` before deployment and that provides reusable
coverage for the project's recurring schema/permission failures.

- Static or deterministic contract coverage rejects production API references
  to explicitly retired database surfaces.
- A local-Supabase integration path exercises the production orchestrator
  context reader against the migrated schema without provider calls.
- Authorization coverage distinguishes user-scoped RLS, service-role, and
  least-privilege direct-role expectations.
- The suite produces actionable failure messages and is documented with exact
  commands.
- No retired schema is restored and no provider-backed generation is invoked.

## Context and source-of-truth documents

`AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`,
`docs/repository-structure.md`, `docs/NORTH_STAR.md`,
`docs/data-model.md`, `docs/scopes/story-spine-unification.md`,
`docs/supabase-identity-and-rls.md`,
`docs/scopes/database-access-boundary.md`,
`docs/scopes/tool-db-interactions.md`,
`docs/agent-system/testing-policy.md`,
`docs/agent-system/false-confidence-audits.md`, and
`docs/agent-system/reviews.md`.

## Research

- Production run `6fce7db3-1e73-4e2b-92b6-cfb0bc6e5226` passed creator-direct
  confirmation and failed before provider execution at
  `orchestratorContext.listPanels`.
- `createSupabaseGraphSnapshotReader()` still reads the retired
  `storyboards`, `storyboard_scenes`, `storyboard_beats`, and
  `storyboard_panels` relations even though the current story spine uses
  `story_blueprints`, `story_blueprint_scenes`, `story_beats`, and
  `story_panels`.
- Existing context-boundary tests inject a fake reader, so they prove tenancy
  filtering but never execute the production Supabase query construction.
- Existing creator-direct role tests are deliberately narrow and do not cover
  the broader service-client/request-client schema contract.
- The independent research review confirmed this incident is schema drift, not
  a rights failure. It also identified stale current-state text in
  `docs/NORTH_STAR.md`, `docs/data-model.md`, and the story-spine scope.

## Plan

1. Add a TypeScript-AST validator with fixture tests that rejects retired
   relation targets in production API `.from()` calls, and include it in API
   agent validation.
2. Repoint all four orchestrator snapshot story readers to the unified story
   spine while preserving the existing snapshot projection contract. This step
   was later superseded by merged PR #840, whose implementation is retained.
3. Add a production-reader query-contract unit test through an injected
   Supabase client factory so the real query construction and row mapping run.
4. Add an opt-in local-Supabase integration that seeds concrete private
   blueprint/scene/beat/panel rows, invokes the production snapshot reader, and
   distinguishes service-role access, owner access, outsider isolation, and
   anonymous isolation.
5. Update authoritative schema/test documentation, run targeted/default/local
   DB checks and the API application smoke, then complete review/validation.

## Decisions

- The stale reader methods must be fixed before this contract PR can merge. PR
  #840 landed that repair independently while this PR was open, so its canonical
  status mapping, database factory, and system-doc wording take precedence in
  conflict resolution.
- Keep direct `popcorn_api` coverage operation-specific; do not broaden that
  least-privilege role to the story spine merely to create a universal rights
  test.
- Keep provider-backed generation outside the suite. Loading context and
  querying RLS rows deterministically proves the pre-tool failure boundary
  without spending credits.

## Changes

- Added an always-on TypeScript-AST production relation validator with an
  explicit retired-surface/replacement catalog, alias/dynamic-call rejection,
  fixture tests, package commands, and API agent-validation wiring.
- Eliminated the six production dynamic Supabase `.from()` calls by routing
  literal-union choices through literal branches.
- Retained the production story-spine reader repair merged through PR #840,
  including its canonical compatibility status mapping and injectable database
  factory; this PR no longer claims ownership of that implementation.
- Strengthened PR #840's production-reader regression with exact selected-column
  assertions for all four story relations while preserving its malformed
  provenance, status-normalization, null/zero-value, and mapping cases.
- Added an opt-in local-Supabase contract with concrete hierarchy fixtures,
  service/owner/outsider/anonymous access assertions, zero durable action/job
  assertions, bounded stack preflight, and leak-resistant cleanup.
- Closed the fully dynamic element-access escape identified in review:
  `db[member](table)` now fails even when neither the method nor relation is
  statically recoverable. The production inventory contains zero legitimate
  dynamic element-call sites.
- Expanded the local contract to all five story-spine relations and the actual
  membership policy matrix: owner/admin/member private reads, outsider/anon
  private isolation, positive outsider/anon public reads, admin/member
  representative panel CRUD, and outsider/anon public-project write denial.
- Registered auth and domain cleanup identifiers as soon as each fixture is
  created, and added an auth-id domain cleanup fallback, so failures before the
  domain lookup completes remain recoverable.
- Added the database-contract testing guide. During conflict resolution,
  retained PR #840's newer `NORTH_STAR` and data-model wording and migration
  references rather than reintroducing this branch's parallel edits.
- Merged `origin/main` at `341e8da3` after PR #840 landed. Kept both opt-in
  integrations because PR #840's empty service-role domain projection and this
  PR's populated multi-role RLS/CRUD matrix prove different boundaries.
- Updated `db:contracts:test:local` to run both complementary integrations as
  the supported local database contract command.

## Validation evidence

- `pnpm db:relations:test` passes 7/7; the repository scan reports 411
  literal production relation calls, zero dynamic calls, and no retired
  relation targets. The fixtures include the fully dynamic
  `db[member](table)` bypass reported in review.
- The graph context boundary + production reader targeted suite passes 9/9.
- `pnpm --filter @popcorn/api typecheck` passes.
- Before the main-branch merge, `pnpm --filter @popcorn/api test` reported 979
  passing, 122 skipped, 3 todo,
  and the same four unrelated baseline failures documented by DB-20260730-01:
  two stale guest-retention migration filenames, the discover UUID fixture,
  and the `delegate_domains` projection expectation.
- A development API served `/api/v1/health` with HTTP 200 in local auth/local
  DB mode and drained on SIGINT. Its background worker logged the expected
  missing-Supabase error because this smoke intentionally had no Supabase keys.
- `pnpm agent:lint:fix` and `pnpm agent:validate -- --scope api` pass.
- Initial `pnpm db:contracts:test:local` attempts did not reach the Node test
  because the local Supabase CLI timed out waiting for Docker. The bounded
  preflight failed loudly after 20 seconds rather than hanging.
- The continuation reran the expanded test in normal mode (one intentional
  skip), the production graph-reader regression (1/1), relation tests (7/7),
  relation validation (411 literal/0 dynamic), API typecheck, lint fix, and API
  agent validation successfully. A second local-contract attempt still stopped
  at the bounded 20-second Docker/Supabase preflight.
- After merging main, `pnpm db:contracts:test:local` runs both database
  integrations and passes 2/2: PR #840's empty creator-direct service-role
  projection and this PR's populated owner/admin/member/outsider/anonymous
  story-spine RLS and representative CRUD matrix.
- The post-merge targeted context suite passes 9 with both integrations skipped
  in normal mode. The full API suite reports 979 passing, 123 skipped, 3 todo,
  and the same four unrelated baseline failures; the extra skip is PR #840's
  opt-in integration.
- The post-merge development API served `/api/v1/health` with HTTP 200 on port
  4317 in local auth/local DB mode and drained cleanly on SIGINT. The background
  worker logged the expected missing-Supabase error for this configuration.

## Independent reviews

- Research: confirmed the incident is schema drift, not a rights failure;
  identified all four stale parallel reads, the current story-spine authority,
  stale system docs, and the need to fix the production reader in the same PR
  for honest green tests.
- Plan: approved the static + production-reader + local RLS layers, required a
  concrete act/scene/beat/panel fixture, service-role fixture proof before
  interpreting empty RLS results, and operation-specific direct-role tests.
- Implementation: initially found dynamic-call allowlisting could conceal a
  retired relation, local cleanup leaked domain users, and the recording test
  under-asserted selected columns. Resolved by eliminating dynamic `.from()`
  calls, independently cleaning domain/auth users, bounding the local-stack
  preflight, and asserting exact selects/full mappings. Re-review found no
  remaining correctness or security blockers.
- Continuation research/plan: confirmed the fully dynamic review finding,
  found zero legitimate production dynamic element-call sites, verified the
  five story-table policies share membership-based `owns_project` semantics,
  and approved representative panel CRUD plus a read-only hosted-smoke
  boundary.
- Continuation implementation: found one partial-setup cleanup leak between the
  auth trigger and domain-id lookup. Resolved by deleting domain rows through
  each registered auth id before id-based and auth-user cleanup; re-review
  approved the guard, role/public matrix, write denials, cleanup, and docs with
  no remaining correctness or security blockers.
- Merge implementation: approved the canonical main reader/docs and combined
  unit test after identifying two follow-through gaps: an overstated unit-test
  claim and a local contract script that ran only one integration. Resolved by
  correcting the guide and wiring both integrations into
  `pnpm db:contracts:test:local`.
- Wrap-up: approved with no blockers after independently rerunning API agent
  validation and checking the 411/0 relation inventory, targeted/full-suite
  evidence, explicit local-integration limitation, documentation, commands,
  feedback record, and clean diff.
- Continuation wrap-up: approved with no blockers after checking the final
  six-file diff, successful API validation, exact static/recording evidence,
  honest skipped/local-timeout reporting, remaining hosted-smoke boundary, and
  updated worksheet/feedback record.
- Merge wrap-up: approved with no blockers after verifying `MERGE_HEAD` matches
  `origin/main` at `341e8da3`, all conflicts are resolved and staged, both local
  database contracts pass, API health is live, the full-suite failures match
  baseline, and final lint/API agent validation pass.

## Blockers and risks

- A broad source scan can produce false positives from migrations, tests, and
  historical documentation; scope it to production runtime code and keep the
  retired-surface catalog explicit.
- Service-role success does not prove request-RLS access, while request-RLS
  denial does not imply a missing schema object. Test these boundaries
  separately.
- Local integration must not invoke model or media providers.
- A hosted schema-cache smoke remains intentionally unimplemented until an
  approved production test project/session is available; it must be read-only
  and cannot substitute for the local RLS matrix.

## Next action / handoff

Ready-for-review PR:
[kmgrassi/PopcornReady#841](https://github.com/kmgrassi/PopcornReady/pull/841).

The real local schema/RLS matrix now passes. A future hosted schema-cache smoke
still requires an explicitly approved production test project/session.
