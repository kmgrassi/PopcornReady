# Worksheet: TYPE-20260716-01

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/TYPE-20260716-01 as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

Replace an unsafe JSON metadata cast in the local object store with a runtime
type guard, and add a regression test proving malformed metadata is ignored.
The API typecheck, targeted storage test, agent validation, commit, and open PR
must all succeed.

## Context and source-of-truth documents

- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/agent-system/worksheets-and-feedback.md`
- `docs/agent-system/reviews.md`

## Decisions

- Scope this run to `apps/api/src/lib/storage/local-store.ts` and its focused
  test. The local JSON sidecar is an untrusted filesystem boundary.
- Treat only objects with an optional string `contentType` as valid metadata;
  invalid JSON or invalid shapes fall back to empty metadata.
- No independent reviewer command is configured in this environment; record
  that at the review checkpoints.

## Changes

- `readMetadata` now parses JSON as `unknown` and accepts it only after a
  runtime `contentType` shape check.
- Added a local-store regression test for a numeric `contentType` sidecar.
- Added feedback entry `TYPE-20260716-01`.

## Validation evidence

- `pnpm install --frozen-lockfile` — passed; worktree dependencies were absent.
- `pnpm --filter @popcorn/api exec tsx --test src/lib/storage/__tests__/local-store.test.ts` — passed, 3/3.
- `pnpm --filter @popcorn/api typecheck` — passed.
- `pnpm agent:lint:fix` — passed.
- `pnpm agent:validate -- --scope api` — passed.
- `git diff --check` — passed.
- The package-wide API test command ran 932 tests and had 3 unrelated failures:
  two missing guest-retention migration fixtures and one existing UUID-shaped
  project-id expectation.

## Independent reviews

Research/plan/implementation/wrap-up: unavailable because
`AGENT_REVIEW_COMMAND` is not configured.

## Blockers and risks

None known.

## Next action / handoff

Commit, push, and open the ready-for-review PR.
