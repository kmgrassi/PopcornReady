# Worksheet: API-20260715-01

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/API-20260715-01 as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

Refactor one active codebase file over 1,000 lines into smaller components, keep behavior unchanged, run targeted validation plus the affected API app path, commit the work, and open a ready-for-review PR.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/scopes/api-contract-v1.md`
- `docs/scopes/north-star-store-consolidation.md`

## Decisions

- Targeted `apps/api/src/lib/api/v1/store.ts` because it is the only active `apps`/`packages` TypeScript file over 1,000 lines, at 6,576 lines before this task.
- Chose asset media URL projection as the extraction boundary because it is already separately tested, has a narrow row-shaped input, and does not require route behavior changes.
- Kept DB-scoped `getAssetMediaUrls` in `store.ts`; moved pure URL/rendition projection to a new helper module.

## Changes

- Added `apps/api/src/lib/api/v1/asset-media-urls.ts` with the extracted media URL and thumbnail rendition projection helper.
- Updated `apps/api/src/lib/api/v1/store.ts` to import and re-export the extracted helper/types while keeping DB-scoped reads in the store.
- Updated `apps/api/src/lib/api/v1/__tests__/asset-media-urls.test.ts` to test the extracted module directly.

## Validation evidence

- `pnpm install --frozen-lockfile` to initialize the fresh worktree dependencies from the lockfile.
- `pnpm --filter @popcorn/api exec tsx --test src/lib/api/v1/__tests__/asset-media-urls.test.ts` passed: 5 tests.
- `pnpm --filter @popcorn/api typecheck` passed.
- `PORT=4107 AUTH_MODE=local NODE_ENV=development pnpm --filter @popcorn/api start` ran the API app path; `curl -fsS http://127.0.0.1:4107/api/v1/health` returned `{"status":"ok","authMode":"local",...}`. The orchestrator worker logged expected missing Supabase env errors in this local worktree before shutdown.
- `pnpm agent:lint:fix` passed.
- `pnpm agent:validate -- --scope api` passed.

## Independent reviews

- Research checkpoint: sub-agent `019f6654-aeb7-7022-af63-1ccd866896a8` mapped `store.ts` and recommended a larger script-draft extraction; this task chose the smaller media URL extraction to keep the daily refactor behavior-preserving.
- Plan checkpoint: sub-agent `019f6656-5fb1-7031-80d5-6b11f354541a` agreed the media URL boundary was coherent and called out typecheck and API smoke requirements.
- Implementation checkpoint: sub-agent `019f6658-3ac8-7bb2-afe6-10fda0a94985` found the untracked helper, a narrowed exported row type, and missing record updates. The row type was widened, records were updated, and the helper will be staged with the commit.
- Wrap-up checkpoint: sub-agent `019f665b-7fed-7a20-bbe2-67043d88af9e` found no release blockers and confirmed the task files were PR-ready after staging.

## Blockers and risks

- The API store still remains large after one focused extraction; this automation intentionally keeps the daily change reviewable.

## Next action / handoff

Commit the implementation and records, tag `worksheet/API-20260715-01`, and open a ready-for-review PR.
