# Worksheet: WEB-20260716-01

<!-- agent-summary: Durable record for the project-poster media viewer task. -->
<!-- agent-summary: The Inspiration story poster should open the shared full-screen MediaViewer. -->
<!-- agent-summary: Ready poster media gains a keyboard-accessible view action. -->
<!-- agent-summary: Coverage belongs in a mocked Playwright Inspiration fixture. -->
<!-- agent-summary: Browser verification covers dialog visibility and Escape dismissal at desktop and mobile widths. -->
<!-- agent-summary: Documentation records Inspiration poster viewer coverage in the E2E inventory. -->
<!-- agent-summary: Independent research, implementation, and wrap-up reviews are recorded below. -->

## Goal and acceptance criteria

- Clicking a healthy Inspiration story poster opens the central `MediaViewer`.
- The viewer shows the story poster and closes with Escape.
- A targeted browser test proves the observable behavior and the E2E inventory records its coverage.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, `docs/repository-structure.md`
- `apps/web/PRODUCT.md`, `apps/web/DESIGN.md`, `docs/ui-interaction-model.md`
- `docs/testing/e2e-test-inventory-and-gaps.md`, `apps/web/e2e/README.md`

## Decisions

- Reuse `apps/web/src/components/media/MediaViewer.tsx`; do not introduce a second lightbox.
- Keep viewing separate from content changes: the image activates the viewer while `Request changes` remains its existing object-scoped agent action.
- Treat the Vite E2E poster flag as an explicit string opt-in; its pure predicate is unit-tested for unset, `"false"`, and `"true"` values.

## Changes

- `InspirationPage` now supplies a ready poster as a labeled button and opens the shared viewer with its existing URL.
- `inspiration-poster.spec.ts` mocks a ready poster, opens the viewer at desktop and mobile widths, and verifies Escape closes it.
- `InspirationPage.module.css` retains the poster presentation while adding the focus ring and zoom cursor for its view action.
- `playwright.config.ts` exposes poster generation only to the mocked E2E server, leaving local development poster generation disabled.
- `docs/testing/e2e-test-inventory-and-gaps.md` records the route-level viewer coverage.
- The PR review fix treats the E2E opt-in as enabled only when its Vite string value is exactly `"true"`.
- `inspiration-poster-generation.test.ts` prevents `"false"` from enabling poster generation.

## Validation evidence

- `pnpm install --frozen-lockfile` restored the lockfile-defined workspace dependencies in this worktree.
- `pnpm --filter @popcorn/web typecheck` passed.
- `pnpm --filter @popcorn/web exec playwright test e2e/inspiration-poster.spec.ts --project chromium --project mobile-safari --project mobile-chrome` passed (3/3): the ready story poster opened `MediaViewer`, rendered its URL, and dismissed via Escape at desktop and mobile viewports.
- The same focused test and `pnpm --filter @popcorn/web typecheck` passed after the PR comment fix.
- `pnpm --filter @popcorn/web test -- inspiration-poster-generation.test.ts` passed (33/33), including the explicit Vite flag regression case.
- `pnpm agent:lint:fix` and `pnpm agent:validate -- --scope all` passed after all implementation, test, worksheet, feedback, and documentation updates.

## Independent reviews

- Research/plan: `/root/research_review` identified the existing `MediaViewer` and confirmed the bare `/inspiration` poster as the affected surface.
- Implementation: `/root/research_review` found no blocking correctness, accessibility, test, or scope defects.
- Wrap-up: `/root/research_review` approved the final implementation, test, inventory, worksheet, and feedback records with no blockers.
- Review-comment fix: `/root/review_comment_check` approved the strict opt-in helper and its unset/`"false"`/`"true"` regression test with no blockers.

## Blockers and risks

- None identified.

## Next action / handoff

- Commit this change set, tag it `worksheet/WEB-20260716-01`, and create a ready-for-review PR.
