# Worksheet: API-20260716-05

<!-- agent-summary: Durable record for one bounded task. -->
<!-- agent-summary: Update this file as evidence arrives, then commit it with the work. -->
<!-- agent-summary: Use worksheet/API-20260716-05 as the git tag after completion. -->
<!-- agent-summary: Do not include secrets, private prompts, or customer data. -->
<!-- agent-summary: A successor agent should be able to continue from this document alone. -->
<!-- agent-summary: Keep command outcomes factual; do not imply checks that did not run. -->
<!-- agent-summary: Link related reviews, feedback entries, and PRs. -->

## Goal and acceptance criteria

Refactor one active codebase file over 1,000 lines into smaller modules, keep
behavior unchanged, validate the affected API path, and open a ready-for-review
PR.

## Context and source-of-truth documents

- `AGENTS.md`
- `AGENT_WORKFLOW.md`
- `CLAUDE.md`
- `docs/repository-structure.md`
- `docs/supabase-identity-and-rls.md`
- `docs/agent-system/README.md`
- `docs/agent-system/worksheets-and-feedback.md`

## Decisions

- Chose `apps/api/src/routes/v1/orchestrator-runs.ts` because it was 1,110
  lines, active API code, and already covered by route-focused tests.
- Kept `orchestrator-runs.ts` as the route owner and compatibility facade.
- Extracted board-revision parsing/resume helpers into
  `orchestrator-run-board-revisions.ts`.
- Extracted restart-from-stage helper math into `orchestrator-run-restarts.ts`.
- Preserved existing test-facing exports from `orchestrator-runs.ts`.

## Changes

- `apps/api/src/routes/v1/orchestrator-runs.ts` reduced to 875 lines.
- Added `apps/api/src/routes/v1/orchestrator-run-board-revisions.ts`.
- Added `apps/api/src/routes/v1/orchestrator-run-restarts.ts`.

## Validation evidence

- `pnpm install --frozen-lockfile` passed after the worktree initially lacked
  `node_modules`.
- `pnpm --filter @popcorn/api exec tsx --test src/routes/v1/__tests__/orchestrator-runs.test.ts src/routes/v1/__tests__/restart-from-stage.test.ts src/routes/v1/__tests__/orchestrator-run-observability.test.ts`
  passed: 43 tests.
- `pnpm --filter @popcorn/api typecheck` passed.
- `NODE_ENV=development AUTH_MODE=local PORT=4105 pnpm --filter @popcorn/api dev`
  started the Express API.
- `curl -sS -i http://127.0.0.1:4105/api/v1/health` returned HTTP 200.
- `curl -sS -i http://127.0.0.1:4105/api/v1/projects/project_missing/generation-runs`
  reached the protected route stack and returned the expected local environment
  error because Supabase service env vars are not configured in this worktree.
- `pnpm agent:lint:fix` passed after adding the required feedback summary
  header.
- `pnpm agent:validate -- --scope api` passed.

## Independent reviews

- Research checkpoint: Descartes confirmed `orchestrator-runs.ts` was a
  suitable low-risk target and recommended focused route tests.
- Plan checkpoint: Heisenberg approved helper extraction with facade re-exports
  and `.js` ESM import specifiers.
- Implementation checkpoint: Zeno found no import/export, behavior drift, or
  circular/import-time risks; noted the two new helper files must be included in
  the commit.

## Blockers and risks

- Initial test/typecheck commands failed before dependency bootstrap because the
  fresh worktree lacked `tsx` and `tsc`.
- No behavior changes intended; risk is limited to import/export wiring.

## Next action / handoff

Commit, tag `worksheet/API-20260716-05`, push, and open a ready-for-review PR.
