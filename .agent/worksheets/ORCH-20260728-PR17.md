# Worksheet: ORCH-20260728-PR17

<!-- agent-summary: Durable record for the hierarchical production run projection. -->
<!-- agent-summary: PR 17 turns root and specialist runtime state into a creator-safe run detail. -->
<!-- agent-summary: Production progress narrates creative delegation rather than a numbered tool pipeline. -->
<!-- agent-summary: Root-origin questions remain owned by the creative director, while direct work stays in Asset Studio. -->
<!-- agent-summary: The projection must not expose raw task payloads, reasoning, provider diagnostics, or actor metadata. -->
<!-- agent-summary: The worktree starts from GitHub PR 823, the completed PR 16 parallel-dispatch implementation. -->
<!-- agent-summary: API, web, desktop/mobile browser, and E2E inventory checks are required before handoff. -->

## Goal and acceptance criteria

Implement roadmap PR 17: add a creator-safe hierarchical root-run projection and an observe-first production UI that makes Creative Director delegation, Visuals/Audio session state, outcomes, and root approval legible without presenting primitives or direct-edit controls as the main flow.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, and `docs/repository-structure.md`.
- `docs/NORTH_STAR.md`, `docs/ui-interaction-model.md`, and PR 17 in `docs/scopes/specialist-agent-orchestration-prs.md`.
- GitHub PR #823 (`5b21ffba`), the PR 16 dependency.
- `apps/api/src/lib/api/v1/domain-session-store.ts`, `apps/api/src/routes/v1/orchestrator-runs.ts`, and `apps/api/src/routes/v1/orchestrator-run-projections.ts`.
- `apps/web/PRODUCT.md`, `apps/web/DESIGN.md`, and the existing run-progress route/components.

## Decisions

- Start the isolated PR 17 branch at PR #823's head, not at stale `main`.
- Preserve standalone creator-direct run APIs and keep them out of the production root conversation surface.
- Keep immutable predecessor runs visible for audit, but derive root question and session state only from continuation leaves.
- Expose report outcomes and output asset IDs, never a report action ID, raw question, evidence, origin, task, or provider metadata.

## Changes

- Added an additive creator-safe hierarchy to root generation-run detail, grouping finite Visuals/Audio runs by persistent session and projecting sanitized primitive job activity.
- Addressed PR #824 review feedback: continuation successors clear predecessor question/failure state, queued provider jobs stay queued, and report action IDs are not public.
- Added behavior-focused projection tests for continuation recovery, public-data sanitization, and queued-job state.

## Validation evidence

- GitHub PR #823 inspected: open, mergeable, and includes PRs 13–16 through `5b21ffba`.
- `pnpm --filter @popcorn/api typecheck` — passed after the PR #824 feedback changes.
- `pnpm --filter @popcorn/api exec tsx --test src/routes/v1/__tests__/session-run-projection.test.ts` — passed (continuation/sanitization and queued-job behavior).

## Independent reviews

- Research/plan review requested against the PR #823 base.
- PR #824 automated review: five actionable threads addressed in the follow-up commit.

## Blockers and risks

- The existing run detail is a legacy stage/tool projection. The new hierarchy DTO must be additive and sanitize service-only metadata.

## Next action / handoff

- Complete the projection/UI design, implement it in small increments, and validate the API and desktop/mobile production route.
