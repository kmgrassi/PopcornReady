# Worksheet: ORCH-20260728-PR15

<!-- agent-summary: Durable record for specialist-agent orchestration PR 15. -->
<!-- agent-summary: PR 15 routes production Request Changes through graph-scoped root decisions. -->
<!-- agent-summary: It depends on PRs 12 and 14 plus graph-rerun and immutable regeneration contracts. -->
<!-- agent-summary: The isolated branch is based on the PR 14 integration branch for stacked delivery. -->
<!-- agent-summary: Do not duplicate prerequisite APIs while their owning PRs are in progress. -->
<!-- agent-summary: Validation must cover graph proposals, origin isolation, session reuse, and Request Changes UI behavior. -->
<!-- agent-summary: Independent reviews are recorded as the implementation and validation evidence arrives. -->

## Goal and acceptance criteria

Implement roadmap PR 15 from `docs/scopes/specialist-agent-orchestration-prs.md`:

- Route production object-scoped Request Changes through a revived root turn with stable targets and current provenance.
- Use graph candidates and creative-director pruning to propose a fingerprinted, costed plan before expensive work.
- Preserve unaffected selections, reuse persistent domain sessions, and retain existing approval/rejection/selection carve-outs.
- Extend consumed creator-direct output changes only through the same graph-scoped proposal and reconciliation contract.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`, and `docs/repository-structure.md`.
- `docs/scopes/specialist-agent-orchestration-prs.md` PR 15.
- `docs/scopes/graph-rerun-decisioning-prs.md` PRs 1, 2, 4, and 5.
- `docs/scopes/regeneration-coverage-prs.md` PRs 1–6.
- `docs/ui-interaction-model.md`, `apps/web/PRODUCT.md`, `apps/web/DESIGN.md`, and E2E guidance.

## Decisions

- Created isolated branch `codex/pr15-request-changes` from `codex/specialist-pr14-creative-director`, the intended stacked base.
- Do not build a competing rerun-proposal API or speculative PR 12/14 adapters. The roadmap assigns those prerequisite contracts to separate PRs.
- Fast-forwarded the branch to merged creative-director/Asset Studio integration
  commit `b02a9d5b` after GitHub PR #819 merged. The base now includes PRs 12,
  13, and 14.

## Changes

- Created this worksheet.
- Added `RerunProposal.v1` shared contract and a protected proposal route.
- Added a server-owned deterministic proposal service that records a proposed
  action, derives graph candidates and content/selection freshness pins, and
  requires approval for every user request.
- Limited the currently executable proposal kind to immutable image
  regeneration coverage; proposals remain preview-only until an executor can
  revalidate their pins. All other kinds are explicitly unavailable rather than
  falling back to fixed-stage restart behavior.

## Validation evidence

- Read and mapped the governing scopes and current Request Changes, graph-candidate, restart, and domain-session seams.
- Verified PR #819 merged its creative-director profile into the PR-12 integration
  branch. The worktree now contains `agent-creations`, the Asset Studio, the
  creative-director root profile, and root graph projection.
- The graph-rerun proposal and kind-specific immutable regeneration contracts
  are still absent; the implementation must not duplicate their owned APIs.
- `pnpm install --frozen-lockfile` completed in this isolated worktree.
- `pnpm --filter @popcorn/api exec tsx --test src/lib/orchestrator/__tests__/rerun-proposal-service.test.ts` passed (2 tests).
- `pnpm --filter @popcorn/api typecheck` and `pnpm --filter @popcorn/shared typecheck` passed.
- After implementation review fixes, the focused test and API typecheck passed
  again and `git diff --check` passed.

## Independent reviews

- Research — `/root/research_review`: confirmed missing prerequisites; advised waiting for the owning implementations rather than duplicating the graph proposal or root/profile contracts. No code changes reviewed yet.
- Plan — `/root/research_review`: the merged PR 12–14 seams can support root
  Request Changes, but graph-rerun PRs 1–5 and regeneration-coverage PRs 1–6
  are still wholly absent. The proposed work requires a new shared proposal
  contract, proposal service/router, root-only run selection, proposal-aware UI,
  and extension of delegation pins/targets. The reviewer flagged this as a
  material prerequisite gap, not a small adapter omission.
- Implementation — `/root/research_review`: identified unsafe executable-state,
  missing target selection pins, and permissive root-run validation. Resolved by
  making all proposals preview-only, pinning the target's active selections, and
  requiring an explicitly supplied creative-director run.

## Blockers and risks

- Current code still routes Request Changes through legacy `board_feedback` and
  chooses any usable project run. PR 15 must select/revive the root run without
  allowing creator-direct or domain history to impersonate root production work.
- The graph-rerun proposal and kind-specific immutable regeneration contracts
  remain absent. Their execution/fallback APIs must be consumed once available,
  not recreated in this branch.

## Next action / handoff

- Await direction on whether this PR should absorb the missing graph-rerun and
  regeneration foundation (a material scope expansion) or remain correctly
  stacked on those owning PRs. Once resolved, implement the reviewed plan with
  focused tests and browser verification.
