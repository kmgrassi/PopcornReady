# Worksheet: QA-20260714-01

<!-- agent-summary: Production verification record for merged GitHub PRs 782, 783, and 784. -->
<!-- agent-summary: Browser checks cover only production-safe reads and existing guest fixture data. -->
<!-- agent-summary: No provider generation, retry, feedback, approval, cancellation, or settings mutation was submitted. -->
<!-- agent-summary: The detailed pass ran at 08feca51 and current-head refresh matched eb2245d6. -->
<!-- agent-summary: Shared contracts, capability metadata, recovery projection, and async resume suites passed. -->
<!-- agent-summary: Lease fencing and role enforcement passed against the existing local Supabase stack. -->
<!-- agent-summary: Dormant specialist behavior and unrelated main-suite failures remain explicitly separated from verified behavior. -->

## Goal and acceptance criteria

Verify that the production deployment contains merged PRs 782, 783, and 784,
exercise the relevant safe browser-visible regression surfaces from the
repository's manual-test guides, and run the automated checks that own the
non-visual contract, registry, recovery, and async-resume guarantees. Avoid
explicit mutation controls and billable production work; ordinary route loads
may record project activity. Record exact limits and open a ready QA PR.

Acceptance requires the expected deployed commit, healthy production reads,
clean public/authenticated route rendering at desktop and mobile widths, no
exposed production tool-test harness, green affected tests, an independent plan
and wrap-up review, and accurate documentation of anything not exercised.

## Context and source-of-truth documents

- `AGENTS.md`, `AGENT_WORKFLOW.md`, `CLAUDE.md`
- `docs/repository-structure.md`, `docs/NORTH_STAR.md`
- `docs/scopes/specialist-agent-orchestration-prs.md`
- `docs/domain-agent-orchestration-contract.md`
- `docs/testing/e2e-test-inventory-and-gaps.md`
- `docs/manual-tests/full-app-inventory.md`
- `docs/manual-tests/browser-flow-backlog.md`
- `docs/manual-tests/orchestrator-tool-calls.md`
- `apps/web/e2e/README.md`
- `apps/api/src/lib/tool-tests/README.md`

## Decisions

- Treat PR 782 as a compile-time contract change with no browser-specific
  behavior.
- Treat PR 784's flat projection parity as observable, but do not present its
  dormant role registries or recovery projector as activated production agents.
- Use the existing guest fixture for read-only dashboard, project, and run
  inspection. The run-detail GET performs its normal project-activity recording;
  do not submit any retry, restart, feedback, review, generation, or account
  action.
- Do not invoke the manual provider endpoints or real-model smoke commands;
  those are billable, stateful, or both.
- Verify PR 783's race and tenancy properties with targeted unit tests plus the
  local-Supabase integration test because no safe live job completion was
  available.
- Record full-suite failures outside the merged delta instead of expanding this
  QA task into unrelated fixes.

## Changes

- Added a dated production-verification section and concrete remaining gaps to
  `docs/testing/e2e-test-inventory-and-gaps.md`.
- Migrated that materially touched system document to the seven-line summary
  contract.
- Added this worksheet and the corresponding feedback entry.

## Validation evidence

Production browser:

- `/api/v1/health` returned 200 with `status: ok`, Supabase auth mode, and the
  specialist merge snapshot `08feca51cdadd302f6e5590a222b2ce9e1157d3b`
  during the detailed pass.
- `/api/v1/dev/tool-tests` did not expose batteries and returned the production
  auth guard rather than the dev harness.
- Landing, login, signup, unknown route, dashboard, Library projects, Activity,
  project-creation entry, and an existing failed-run route rendered without
  console warnings or unexpected 4xx/5xx responses.
- Empty login/signup forms kept their primary submissions disabled.
- The failed run showed a readable terminal failure, 50% progress, completed
  storyboard content, stage labels, and recovery controls without any action
  being submitted.
- Dashboard and run detail had no document-level horizontal overflow under a
  390-by-844 mobile emulation.
- After `main` advanced, an independent browser refresh verified health, landing,
  and authenticated dashboard on current deployed commit
  `eb2245d670422db56faa0fdb0fe8034d28779a8a`. The refresh had no console
  warnings/errors or unexpected 4xx/5xx responses. Its authenticated session
  redirected `/login` to `/dashboard`, so it did not repeat the signed-out empty
  login-form assertion.
- Current-head Railway verification run `29418157873` and Web E2E run
  `29418157957` succeeded for `eb2245d6`.

Merged-main automated checks:

- `pnpm --filter @popcorn/shared typecheck` — passed.
- `pnpm --filter @popcorn/shared test:types` — passed.
- Focused catalog, restricted-registry, bridge, recovery, and run-projection
  suite — 27 passed, 0 failed.
- Focused engine, retry, recovery-worker, and dispatch-migration suite — 34
  passed, 0 failed, 1 local database integration test skipped by default.
- Local Supabase migration history included `20260714143000`.
- GitHub Actions `Apply Supabase migrations` run `29358219869` succeeded for PR
  783's merge commit `30601df2`.
- Explicit local-Supabase dispatch integration — 1 passed, 0 failed; verified
  anonymous/authenticated RPC denial, service-role wake, lease preservation,
  requeue, and workspace-drift rejection.
- `pnpm --filter @popcorn/api typecheck` — passed.
- Full `pnpm --filter @popcorn/api test` — 723 passed, 3 failed, 61 skipped, 3
  todo. The three failures are unrelated merged-main baseline failures: two
  guest-retention tests still reference the pre-renumbering `...120000...` and
  `...150000...` migration paths while `main` contains `...120100...` and
  `...150100...`, and the public-project UUID assertion fails. None of their
  tests or source files changed in PRs 782-784.
- `pnpm agent:lint:fix` — passed over the three changed files.
- `pnpm agent:validate -- --scope docs` — passed.
- `pnpm agent:validate -- --scope api` — passed, including API typecheck.

## Independent reviews

- Research/plan review `/root/merged_diff_test_map` separated safe production
  evidence from automated-only and intentionally dormant behavior. It also
  identified the missing direct worker-wake assertion for video edit jobs.
- Manual-test safety review `/root/manual_test_safety_review` confirmed that
  provider manual endpoints, generation entrypoints, review actions, settings
  writes, and real-model tool smokes must not be invoked in this pass.
- Wrap-up review `/root/merged_diff_test_map` initially found missing validation
  bookkeeping, omitted hosted migration evidence, an overbroad browser-scope
  claim, imprecise GET wording, and ambiguous migration-failure attribution.
  Each finding is resolved in this revision.
- Wrap-up review `/root/qa_wrap_review` then found that `main` and production had
  advanced by four commits. The QA branch was fast-forwarded, both feedback-log
  entries were preserved, and the current-head CI and browser smoke above were
  refreshed. Its final rereview returned READY with no remaining findings.

## Blockers and risks

- A live production async completion was not available without starting or
  retrying billable work. Production rendering proves projection/regression
  health, while concurrency behavior is evidenced by the affected automated
  suites and local database integration.
- The initial guest session ended after the public auth route checks. A later
  current-head refresh used an independently authenticated dashboard session,
  but did not revisit the same guest fixture or run.
- The role-specific registries and recovery projector remain dormant by design;
  their presence must not be mistaken for an activated specialist hierarchy.
- The unrelated full API suite failures keep merged `main` from being globally
  green even though every affected suite passed.

## Next action / handoff

Commit and tag the evidence, push the branch, and open a ready PR.
