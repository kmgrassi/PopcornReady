# Feedback: API-20260715-01

<!-- agent-summary: Feedback for the API-20260715-01 large-store refactor. -->
<!-- agent-summary: The task extracted asset media URL projection from the v1 API store. -->
<!-- agent-summary: Small pure projections make safe seams in large persistence files. -->
<!-- agent-summary: The store remains the database and tenancy boundary after extraction. -->
<!-- agent-summary: Exported helper row types should preserve compatibility with old store types. -->
<!-- agent-summary: Future store reductions can target script drafts with DB-backed tests. -->
<!-- agent-summary: Keep feedback records task-scoped and committed with worksheets. -->

## Lesson

Small extractions from very large store modules work best when the new module owns a pure projection and the original store remains the tenancy and database boundary. Preserve exported helper types exactly enough for current and future callers, even when the moved code only reads a subset.

## Follow-up

Continue reducing `apps/api/src/lib/api/v1/store.ts` with similarly coherent seams; script draft persistence is a candidate for a future, larger DB-backed extraction with its own persistence tests.
