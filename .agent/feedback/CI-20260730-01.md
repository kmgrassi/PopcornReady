# Feedback: CI-20260730-01

<!-- agent-summary: GitHub Actions savings should be measured in runner time, not action-step count. -->
<!-- agent-summary: Web E2E currently dominates repository runner usage. -->
<!-- agent-summary: Canceling superseded verification preserves coverage while removing stale work. -->
<!-- agent-summary: Production database mutation workflows must remain non-cancelable. -->
<!-- agent-summary: Workflow-level path filters complicate future required-check configuration. -->
<!-- agent-summary: Failure-only artifacts retain useful diagnostics without storing success reports. -->
<!-- agent-summary: Workflow policy tests complement but do not replace GitHub schema validation. -->

## Observation

The initial request described reducing the number of GitHub Actions used per
run, but recent history showed that marketplace action steps were negligible
compared with browser-test and obsolete polling runner time.

## Improvement applied

Optimize the expensive workflows around latest-commit semantics: cancel
superseded browser/deployment verification, skip non-runtime Markdown and agent
record changes, bound browser jobs, and retain artifacts only for failures.
Guard the cancellation boundary with a local policy test so Supabase mutations
remain serialized.

PR review exposed a second workflow-level boundary: concurrency is evaluated
before a job-level `if`. A skipped non-production dispatch therefore needs a
different concurrency group from production rather than relying on the job
gate to prevent interference.

## Follow-up

If Web E2E becomes a required check, replace workflow-level path filtering with
a classifier/no-op job before enabling the requirement. Re-measure runner usage
after enough PR and `main` runs accumulate; only then consider splitting the
browser matrix or removing the post-merge run.
