# Feedback: TYPE-20260720-01

<!-- agent-summary: Records the lesson from the API OpenAI JSON boundary hardening task. -->
<!-- agent-summary: Provider JSON remains unknown until validated by an object guard. -->
<!-- agent-summary: The task is intentionally limited to one production adapter boundary. -->
<!-- agent-summary: Existing adapter tests provide observable regression coverage. -->
<!-- agent-summary: Validation results and review availability are recorded with the worksheet. -->
<!-- agent-summary: The feedback is committed with the implementation and worksheet. -->
<!-- agent-summary: Follow-up work should target other provider or storage boundaries separately. -->

## Lesson

When a provider payload crosses `JSON.parse`, annotate it as `unknown` even if a nearby guard already narrows it. This prevents implicit `any` from bypassing the guard during future edits.

## Follow-up

Audit other unannotated `JSON.parse` calls in separate, bounded changes; do not widen this PR beyond the API OpenAI adapter.
