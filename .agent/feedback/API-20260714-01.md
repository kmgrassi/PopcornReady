# Feedback: API-20260714-01

## Lesson

When bridging adjacent asset-graph stages, record the dependency as a graph input
and state whether the change is prompt guidance or a deterministic product-structure link.

## Follow-up

The story-spine scope distinguishes this narrow bridge from the pending relational
scene/beat migration.

When one project pointer can name more than one product surface, consumers that
require a typed asset must fall back to the latest compatible asset-backed row.
