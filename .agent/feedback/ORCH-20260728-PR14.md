# Feedback: ORCH-20260728-PR14

<!-- agent-summary: Task-scoped feedback for the creative-director root profile. -->
<!-- agent-summary: A hierarchy flag must choose both the root surface and runnable specialist roles. -->
<!-- agent-summary: The flat root remains the fallback until the hierarchy rollout gate is cleared. -->
<!-- agent-summary: Root context uses fresh graph projections and durable action history, never session prose as state. -->
<!-- agent-summary: Creator-direct pooled outputs remain distinct from root production truth until explicitly selected. -->
<!-- agent-summary: Deployment flags for durable orchestration must remain stable while flagged work drains. -->
<!-- agent-summary: This record ships with worksheet ORCH-20260728-PR14. -->

## Lesson

A root hierarchy cannot be rolled out by replacing only its prompt: the same
fence must select its registry, graph context, and the specialist roles the
durable worker can execute. Otherwise a root can dispatch an Audio assignment
that no worker will claim.

Fresh graph projection plus root action history keeps the root aware of
immutable assets, selections, gates, pins, costs, and its own delegated report
boundaries without treating creator-direct pool history as approved production
truth. The temporary hierarchy flag is deployment-stable while those durable
runs drain.

## Follow-up

PR 17 should extend the root projection with the creator-visible hierarchical
run/report view; PR 18 owns the default-on rollout and soak criteria.
